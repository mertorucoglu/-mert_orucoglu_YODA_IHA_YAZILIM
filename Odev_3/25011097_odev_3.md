# Python  

## Python Syntax 

### Python sözdizimini çalıştırmak: 

Python sözdizimi Komut Satırına doğrudan yazarak çalıştırılabilir:

``` python  
>>> print("Hello, World!")
Hello, World! 
``` 
### Python Indentation 

- Indentation, bir kod satırının başındaki boşluklara denir.

- Diğer programlama dillerinde indentation sadece kodun okunabilirliği içindir; yani insanın anlamasını kolaylaştırır ama dilin kendisi buna dikkat etmez.

- Fakat Python dilinde indentation çok önemlidir. Python, kod bloklarını belirtmek için indentation kullanır. 

``` python  
if 5 > 2:
  print("Five is greater than two!") 
``` 
ama Python indentation atlanırsa sana bir hata verir.

syntax error: 
``` python  
if 5 > 2:
print("Five is greater than two!") 
```  
Boşluk sayısı programcı olarak sana bana bağlıymış ama en yaygın kullanım 4 space — yine de en az 1 space olmak zorunda.

``` python  
if 5 > 2:
 print("Five is greater than two!") 
if 5 > 2:
        print("Five is greater than two!") 
```   

Aynı block of code içinde aynı sayıda space (boşluk) kullanmak zorundasın, aksi halde Python sana bir error (hata) veriyor.

syntax error: 
``` python  
if 5 > 2:
 print("Five is greater than two!")
        print("Five is greater than two!")
```  
## Python Output / Print 

### Print Text 

print() function’ı kullanarak text  veya output values  gösterilebilinir: 

``` python  
print("Hello World!")
```  

print() function’ını istediğin kadar kullanabilirsin.each call varsayılan olarak text’i yeni bir satıra yazdırır.

``` python  
print("Merhaba")
print("Python")
print("Dünyası")
```    

### Çift Tırnak 

Python'da metinler (text) tırnak işaretleri içinde olmalıdır.

``` python  
print("This will work!")
print('This will also work!')
```  

Eğer metni tırnak işaretleri içine almayı unutursan, Python hata verir. 

``` python  
print(This will cause an error)
```  
**SyntaxError: invalid syntax.** 

### Yeni Satır Olmadan Yazdırma 

Varsayılan olarak print() fonksiyonu her yazdırmadan sonra yeni bir satır başlatır.
Eğer birden fazla kelimeyi aynı satırda yazdırmak istiyorsanız, end parametresi kulllanılıyor: 

``` python  
print("enter your age:", end=" ")
print("I will print on the same line.")
```  

Daha iyi okunabilirlik için end=" " sonrasına bir boşluk ekle.

### Sayıları Yazdırmak 

Sayıları yazdırırken tırnak işaretlerine gerek yok. 

``` python
print(3)
print(358)
print(50000)
```  
print fonksiyonunn içinde mat problemide yapabilirsin 

``` python
print(3 + 3)
print(2 * 5)
```  
Metin ve sayıları virgülle ayırarak tek bir çıktıda birleştirilebilinir:

``` python
print("I am", 35, "years old.")
``` 

## Python Comments 

- Yorumlar (comments) Python kodunu açıklamak için kullanılabilir.
- Yorumlar, kodun daha okunabilir olmasını sağlar.
- Yorumlar, kodu test ederken çalıştırılmasını engellemek için de kullanılabilir.

### Yorum Oluşturma 

Yorumlar # ile başlar ve Python onları yok sayar. 

``` python
#This is a comment
print("Hello, World!")
```  

Yorumlar bir satırın sonunda da yazılabilir ve Python o satırın geri kalanını yok sayar. 

``` python
print("Hello, World!") #This is a comment
```  

Bir yorum, kodu açıklamak için olmak zorunda değildi Python’IN o kodu çalıştırmasını engellemek için de kullanılabilir.   

``` python
#print("Hello, World!")
print("Cheers, Mate!")
```  

### Çok Satırlı Yorumlar 

Python’da aslında resmi bir çok satırlı yorum (multiline comment) söz dizimi yoktur.
Çok satırlı yorum eklemek için her satırın başına # koymak lazım 

``` python
#This is a comment
#written in
#more than just one line
print("Hello, World!")
```  

Ya da tam olarak tasarlandığı gibi olmasa da, çok satırlı bir string (multiline string) kullanabiliriz.  
Python, değişkene atanmayan string ifadeleri yok saydığı için, koda üç tırnak (''' veya """) içinde bir string ekleyebilir ve yorumu içine yazabiliriz. 

``` python
"""
This is a comment
written in
more than just one line
"""
print("Hello, World!")
```  

"String bir değişkene atanmadığı sürece, Python kodu okur ama yok sayar; böylece çok satırlı bir yorum  oluşturulabilinir.  


## Python Variables 

#### Variables 
Değişkenler (variables), veri değerlerini saklamak için kullanılan kaplardır. 

#### Variable Oluşturma 

Python’da bir değişkeni önceden bildirmek (declare) için özel bir komut yoktur.  
Bir değişken, ilk değer atandığı anda oluşturulur.

``` python
x = 5
y = "John"
print(x)
print(y)
```  

Değişkenlerin belirli bir veri tipi ile önceden tanımlanmasına gerek yoktur ve bir değer atandıktan sonra bile tipi değişebilir:

``` python
x = 4       # x is of type int
x = "Sally" # x is now of type str
print(x)
```  
### Casting 

Bir değişkenin veri tipini belirtmek istiyorsak, bunu casting ile yapılıyor:  

``` python
x = str(3)    # x will be '3'
y = int(3)    # y will be 3
z = float(3)  # z will be 3.0
```   


### Get The Type 

Bir değişkenin veri tipini type() fonksiyonu ile öğreniriz:

``` python
x = 5
y = "John"
print(type(x))
print(type(y))
```  


### Tek Tırnak mı Çift Tırnak mı 

String (metin) değişkenleri, tek tırnak (' ') veya çift tırnak (" ") kullanılarak oluşturulabilinir.

``` python
x = "John"
# is the same as
x = 'John'
```  

### Case Sensitive 

Değişken isimlerinde büyük/küçük harf duyarlılığı(case sensitive) vardır.

- Bu işlem iki değişken oluşturacaktır: 

``` python
a = 4
A = "Sally"
#A will not overwrite a
```  

### Değişken Adları 

Bir değişkenin kısa bir adı (örneğin x veya y) olabilir veya daha açıklayıcı bir ad (örneğin yaş, kutle, toplam_ses) seçilebilir.

Python Değişken Kuralları:

- Bir değişken ismi bir harf veya alt çizgi (_) ile başlamalıdır 
- Bir değişken ismi bir sayı ile başlayamaz
- Bir değişken ismi yalnızca alfasayısal karakterler ve alt çizgi içerebilir (A-Z, a-z, 0-9, _)
- Değişken isimleri büyük/küçük harfe duyarlıdır.
- Bir değişken ismi Python anahtar kelimesi olamaz 

``` python
myvar = "John"
my_var = "John"
_my_var = "John"
myVar = "John"
MYVAR = "John"
myvar2 = "John"
```  

### Birden Fazla Kelimeden Oluşan Değişken İsimleri 

Birden fazla kelimeden oluşan değişken isimleri okunması zor olabilir.  

Okunabilirliğini artırmak için kullanabileceğiniz birkaç teknik var: 

#### Camel Case 

İlk kelime hariç, her kelimenin baş harfi büyük yazılır.

``` python
myVariableName = "John"
``` 
#### Pascal Case 

Her kelime büyük. 

``` python
MyVariableName = "John"
``` 
#### Snake Case 

Her kelime, alt çizgi (_) ile ayrımış

``` python
my_variable_name = "John"
```  
### Çoklu Değişken Atama 

Python, bir satırda birden fazla değişkene değer atamanıza izin verir.

``` python
x, y, z = "Orange", "Banana", "Cherry"
print(x)
print(y)
print(z
``` 

#### Bir Değeri Birden Fazla Değişkene Atamak 

Ayrıca, aynı satırda birden fazla değişkene aynı değeri atanabilir:

``` python
x = y = z = "Orange"
print(x)
print(y)
print(z)
```  

### Bir veri grubunu parçalara ayırmak

Eğer bir listede, tuple’da  vb. birden fazla değer içeren bir veri grubu varsa, Python bu değerleri değişkenlere ayırmana izin verir. Buna unpacking (açma / parçalara ayırma) denir.

``` python
fruits = ["apple", "banana", "cherry"]
x, y, z = fruits
print(x)
print(y)
print(z)
```   

### Değişkenleri Ekrana Yazdırma 

Değişkenleri ekrana yazdırmak için print() kullanıyoruz. 

``` python
x = "Python is awesome"
print(x)
``` 
print() fonksiyonunda, birden fazla değişkeni virgülle (,) ayırarak ekrana yazdırabilirsin. 

``` python
x = "Python"
y = "is"
z = "awesome"
print(x, y, z)
```  

Birden fazla değişkeni ekrana yazdırmak için "+" operatörünü de kullanılabilir. 

``` python
x = "Python "
y = "is "
z = "awesome"
print(x + y + z)
```  
Sayılar için, + karakteri matematiksel bir işlem operatörü olarak çalışır 


``` python
x = 5
y = 10
print(x + y)
```   
print() fonksiyonunda, bir metin (string) ile bir sayısal değeri (number) + operatörüyle birleştirilirse, Python bir hata (error) verir.


``` python
x = 5
y = "John"
print(x + y)
```  

print() fonksiyonunda birden fazla değişkeni ekrana yazdırmanın en iyi yolu, onları virgülle (,) ayırmaktır. Bu yöntem farklı veri tiplerini bile destekler. 

``` python
x = 5
y = "John"
print(x, y)
```  

### Global Değişkenler 

Fonksiyonların dışında oluşturulan değişkenlere global değişkenler denir.  

Global değişkenler, hem fonksiyonların içinde hem de dışında kullanılabilir. 

``` python
x = "awesome"

def myfunc():
  print("Python is " + x)

myfunc()
```   

Eğer bir fonksiyon içinde aynı isimle bir değişken oluşturursan, bu değişken yerel (local) olur ve yalnızca fonksiyon içinde kullanılabilir. Aynı isimdeki global değişken ise global olarak kalır ve orijinal değerini korur


``` python
x = "awesome"

def myfunc():
  x = "fantastic"
  print("Python is " + x)

myfunc()

print("Python is " + x)
```  

Normalde bir fonksiyon içinde değişken oluşturduğunda, bu değişken yerel (local) olur ve sadece o fonksiyon içinde kullanılabilir.
Bir fonksiyon içinde global değişken oluşturmak için global anahtar kelimesini kullanılmalı

``` python
def myfunc():
  global x
  x = "fantastic"

myfunc()

print("Python is " + x)
```  

Ayrıca, bir fonksiyon içinde global bir değişkenin değerini değiştirmek istiyorsan global anahtar kelimesini kullanılmalı

``` python
x = "awesome"

def myfunc():
  global x
  x = "fantastic"

myfunc()

print("Python is " + x)
``` 


## Python Veri Tipleri   


### Built-in Veri Tipleri 

Programlamada, data type önemli bir kavramdır.

Değişkenler (variables) farklı türlerde (types) verileri saklayabilir ve farklı türler farklı işlemler yapabilir.

Python, varsayılan olarak aşağıdaki built-in data types kategorilerine sahiptir:

Text Type:	str  
Numeric Types:	int, float, complex  
Sequence Types:	list, tuple, range  
Mapping Type:	dict  
Set Types:	set, frozenset  
Boolean Type:	bool  
Binary Types:	bytes, bytearray, memoryview  
None Type:	NoneType  

### Veri Tipini Öğrenme 

Herhangi bir object’in data type’ını, type() fonksiyonunu kullanarak öğrenebilirsin:

``` python
x = 5
print(type(x))
``` 

### Veri Türünü Ayarlama 

Python’da, bir variable’a bir value atadığında data type otomatik olarak belirlenir: 


### Python Temel Veri Tipleri Örnekleri

Python’da, bir variable’a bir value atadığında, data type otomatik olarak belirlenir:


| Örnek (Example) | Veri Tipi (Data Type) |
| :--- | :--- |
| `x = "Hello World"` | `str` |
| `x = 20` | `int` |
| `x = 20.5` | `float` |
| `x = 1j` | `complex` |
| `x = ["apple", "banana", "cherry"]` | `list` |
| `x = ("apple", "banana", "cherry")` | `tuple` |
| `x = range(6)` | `range` |
| `x = {"name" : "John", "age" : 36}` | `dict` |
| `x = {"apple", "banana", "cherry"}` | `set` |
| `x = frozenset({"apple", "banana", "cherry"})` | `frozenset` |
| `x = True` | `bool` |
| `x = b"Hello"` | `bytes` |
| `x = bytearray(5)` | `bytearray` |
| `x = memoryview(bytes(5))` | `memoryview` |
| `x = None` | `NoneType` |  


### Belirli Veri Tipini Ayarlama 

Eğer data type’ı özellikle belirtmek istiyorsan, aşağıdaki constructor functions’ları kullanılmalı: 


| Örnek (Example) | Veri Tipi (Data Type) |
| :--- | :--- |
| `x = str("Hello World")` | `str` |
| `x = int(20)` | `int` |
| `x = float(20.5)` | `float` |
| `x = complex(1j)` | `complex` |
| `x = list(("apple", "banana", "cherry"))` | `list` |
| `x = tuple(("apple", "banana", "cherry"))` | `tuple` |
| `x = range(6)` | `range` |
| `x = dict(name="John", age=36)` | `dict` |
| `x = set(("apple", "banana", "cherry"))` | `set` |
| `x = frozenset(("apple", "banana", "cherry"))` | `frozenset` |
| `x = bool(5)` | `bool` |
| `x = bytes(5)` | `bytes` |
| `x = bytearray(5)` | `bytearray` |
| `x = memoryview(bytes(5))` | `memoryview` | 


## Python’da Tür Dönüştürme 

### Bir Değişkenin Türünü Belirtmek


Bazen bir variable’ın data type’ını özellikle belirtmek için casting yöntemi kullanılır.

Python, object-oriented (nesne yönelimli) bir dildir ve bu nedenle veri tiplerini, primitive tipler de dahil olmak üzere, class’lar ile tanımlar.

Dolayısıyla Python’da casting, constructor functions kullanılarak yapılır: 

- **int()** Bir tamsayı literalinden, bir float literalinden (tüm ondalıkları kaldırarak) veya bir string literalinden (string bir tam sayıyı temsil ediyorsa) bir tamsayı sayı oluşturur.  

- **float()** Bir tamsayı literalinden, bir float literalinden veya bir string literalinden (string bir float veya tamsayıyı temsil ediyorsa) Bir ondalıklı sayı oluşturur. 

- **str()** Stringler, tamsayı literalleri ve float literalleri de dahil olmak üzere çok çeşitli veri tiplerinden bir string oluşturur.

``` python
x = int(1)   # x will be 1
y = int(2.8) # y will be 2
z = int("3") # z will be 3
``` 

``` python
x = float(1)     # x will be 1.0
y = float(2.8)   # y will be 2.8
z = float("3")   # z will be 3.0
w = float("4.2") # w will be 4.2
``` 

``` python
x = str("s1") # x will be 's1'
y = str(2)    # y will be '2'
z = str(3.0)  # z will be '3.0'
``` 

## Python Sayılar 

Python’daki üç sayısal (numeric) tür şunlardır: 

- int  
- float  
- complex  

Numeric type değişkenler, onlara bir value atadığında oluşturulur: 

``` python
x = 1    # int
y = 2.8  # float
z = 1j   # complex
```   
Python’da herhangi bir object’in data type’ını doğrulamak için type() fonksiyonunu kullanabilirsin: 

``` python
print(type(x))
print(type(y))
print(type(z))
```  

### Int

Int, yani integer, ondalık içermeyen, pozitif veya negatif tam sayıdır ve uzunluğu sınırsızdır. 

``` python
x = 1
y = 35656222554887711
z = -3255522

print(type(x))
print(type(y))
print(type(z))
```  

### Float 

Float, yani floating point number, bir veya daha fazla ondalık içeren, pozitif veya negatif sayıdır. 

``` python
x = 1.10
y = 1.0
z = -35.59

print(type(x))
print(type(y))
print(type(z))
```  

Float, ayrıca bilimsel gösterim (scientific notation) ile de ifade edilebilir;
burada "e" harfi, 10’un kuvvetini belirtir.  

``` python
x = 35e3
y = 12E4
z = -87.7e100

print(type(x))
print(type(y))
print(type(z))
```  

### Complex 

Complex numbers (karmaşık sayılar), imaginary part (sanal kısım) için "j" harfi kullanılarak yazılır: 

``` python
x = 3+5j
y = 5j
z = -5j

print(type(x))
print(type(y))
print(type(z))
```  

### Veri Tipi Dönüşümü 

Bir veri tipini başka bir tipe dönüştürmek için int(), float(), ve complex() method’larını kullanabilirsin: 

``` python
x = 1    # int
y = 2.8  # float
z = 1j   # complex

#convert from int to float:
a = float(x)

#convert from float to int:
b = int(y)

#convert from int to complex:
c = complex(x)

print(a)
print(b)
print(c)

print(type(a))
print(type(b))
print(type(c))
```  

### Rastgele Sayı 

Python’da random() adında tek başına bir fonksiyon yoktur, ancak random adında bir built-in module bulunur.  
Bu modül, rastgele sayılar oluşturmak için kullanılabilir: 

``` python
import random

print(random.randrange(1, 10))
```   


### Python Random Modülü 

Python’da rastgele sayılar üretmek için kullanabileceğin bir built-in module vardır: random.

random module, çeşitli işlevler (methods) içerir: 


| Metod | Açıklama |
| :--- | :--- |
| `seed()` | Rastgele sayı üreteciyi başlatır |
| `getstate()` | Rastgele sayı üretecinin mevcut internal state’ini döndürür |
| `setstate()` | Rastgele sayı üretecinin internal state’ini geri yükler. |
| `getrandbits()` | Rastgele bitleri temsil eden bir sayı döndürür. |
| `randrange()` | Verilen aralıkta rastgele bir sayı döndürür. |
| `randint()` | Verilen aralıkta rastgele bir sayı döndürür. |
| `choice()` | Verilen sequence’den rastgele bir eleman döndürür. |
| `choices()` | Verilen sequence’den rastgele seçimler yaparak bir liste döndürür. |
| `shuffle()` | Bir sequence alır ve sequence’i rastgele bir sırayla döndürür. |
| `sample()` | Verilen sequence’den bir örneklem (sample) döndürür. |
| `random()` | 0 ile 1 arasında rastgele bir float sayı döndürür. |
| `uniform()` | Verilen iki parametre arasında rastgele bir float sayı döndürür |
| `triangular()` | Verilen iki parametre arasında rastgele bir float sayı döndürür; ayrıca mode parametresi ile iki parametre arasındaki ortalama noktayı belirler. |
| `betavariate()` | Beta dağılımına (istatistikte kullanılan) dayalı rastgele bir float sayı döndürür. |
| `expovariate()` | Üssel dağılıma (Exponential distribution, istatistikte kullanılan) dayalı rastgele bir float sayı döndürür. |
| `gammavariate()` | Gamma dağılımına (istatistikte kullanılan) dayalı rastgele bir float sayı döndürür. |
| `gauss()` | Gaussian dağılımına (olasılık teorilerinde kullanılan, normal dağılım) dayalı rastgele bir float sayı döndürür. |
| `lognormvariate()` | Log-normal dağılımına (olasılık teorilerinde kullanılan) dayalı rastgele bir float sayı döndürür. |
| `normalvariate()` | Normal dağılıma (olasılık teorilerinde kullanılan) dayalı rastgele bir float sayı döndürür. |
| `vonmisesvariate()`| Von Mises dağılımına (yönsel istatistiklerde kullanılan) dayalı rastgele bir float sayı döndürür. |
| `paretovariate()` | Pareto dağılımına (olasılık teorilerinde kullanılan) dayalı rastgele bir float sayı döndürür. |
| `weibullvariate()` | Weibull dağılımına (istatistikte kullanılan) dayalı rastgele bir float sayı döndürür. | 

## Python Strings (Python Metinler) 

Python’da stringler, ya tek tırnak (') ya da çift tırnak (") ile çevrilir.

'hello', "hello" ile aynı anlama gelir.

Bir string literal’i ekrana yazdırmak için print() fonksiyonunu kullanılır: 

``` python
print("Hello")
print('Hello')
``` 

### Tırnak İçinde Tırnak 

Bir string içinde tırnak kullanılabilir, yalnızca içteki tırnaklar, string’i çevreleyen tırnaklarla aynı türde olmamalıdır: 


``` python
a = "Hello"
print(a)
```  

### Çok Satırlı Stringler 

Bir çok satırlı string’i bir değişkene atamak için üç tırnak (''' veya """) kullanılabilir: 

``` python
a = """Lorem ipsum dolor sit amet,
consectetur adipiscing elit,
sed do eiusmod tempor incididunt
ut labore et dolore magna aliqua."""
print(a) 
``` 

Veya üç adet tek tırnak (''') kullanılabilir:

``` python
a = '''Lorem ipsum dolor sit amet,
consectetur adipiscing elit,
sed do eiusmod tempor incididunt
ut labore et dolore magna aliqua.'''
print(a)
``` 

### Stringler Dizidir 

Diğer birçok popüler programlama dilinde olduğu gibi, Python’daki stringler, Unicode karakterlerden oluşan dizilerdir (arrays).

Ancak, Python’da tek karakter için ayrı bir veri tipi (character) yoktur; tek bir karakter uzunluğu 1 olan bir string olarak temsil edilir.

String içindeki karakterlere erişmek için köşeli parantezler [ ] kullanılabilir: 

``` python
a = "Hello, World!"
print(a[1]
``` 

### Bir String Üzerinde Döngü 


Stringler bir dizi (array) olduğundan, bir string içindeki karakterler üzerinde for döngüsü ile dönebiliriz. 

``` python
for x in "banana":
  print(x)
```  
### String'in Uzunluğu 

Bir string’in uzunluğunu almak için len() fonksiyonunu kullanılır:

``` python
a = "Hello, World!"
print(len(a))
``` 

### String Kontrolü

Bir string içinde belirli bir kelime veya karakterin olup olmadığını kontrol etmek için in anahtar kelimesini kullanabiliriz: 

``` python
txt = "The best things in life are free!"
print("free" in txt)
``` 

Bu "if" içindede kullanılabilir: 

``` python
txt = "The best things in life are free!"
if "free" in txt:
  print("Yes, 'free' is present.")
```  

### Belirli bir şeyin var olmadığını kontrol et 

Bir string içinde belirli bir kelime veya karakterin olmadığını kontrol etmek için **not in** anahtar kelimesini kullanılabilir: 

``` python
txt = "The best things in life are free!"
print("expensive" not in txt)
```  

Bunu bir if ifadesi içinde kullanabilirsin: 

``` python
txt = "The best things in life are free!"
if "expensive" not in txt:
  print("No, 'expensive' is NOT present.")
```  

### Python - String Dilimleme 

Bir string’in belirli bir bölümünü almak için slice (dilimleme) sözdizimini kullanılır..

Başlangıç indeksini ve bitiş indeksini iki nokta : ile ayırarak string’in bir kısmını döndürülebilir. 

``` python
b = "Hello, World!"
print(b[2:5])
```   

**Baştan Dilimleme:** 

Başlangıç indeksini boş olursa, dilimleme ilk karakterden başlar: 

``` python
b = "Hello, World!"
print(b[:5])
```   

**Sondan Dilimleme:**

Bitiş indeksi boş olursa, dilimleme string’in sonuna kadar devam eder:  

``` python
b = "Hello, World!"
print(b[2:])
```  

### Negatif İndeksleme 

String’in sonundan başlayarak dilimleme yapmak için negatif indeksler kullanılabilir: 

``` python
b = "Hello, World!"
print(b[-5:-2])
```  

### Python - Stringleri Değiştirmek 

Python’da stringler üzerinde kullanabileceğin hazır (built-in) metotlar vardır.

#### Büyük Harf 

``` python
a = "Hello, World!"
print(a.upper())
```  

#### Küçük Harf 

``` python
a = "Hello, World!"
print(a.lower())
```  

#### Boşlukları Kaldırmak

Boşluk (whitespace), metnin önündeki ve/veya sonundaki boşluk ve çoğu zaman, bu boşlukları kaldırrılır çünkü gereksiz boşluklar veri işleme veya karşılaştırma sırasında sorun yapıyor. 

``` python
a = " Hello, World! "
print(a.strip()) # returns "Hello, World!"
``` 

### String Değiştirme 

replace() metodu, bir string içindeki belirli bir kısmı başka bir string ile değiştirir. 

``` python
a = "Hello, World!"
print(a.replace("H", "J"))
```  

### String Bölme 

split() metodu, belirtilen ayırıcıya göre metni parçalara ayırır ve bu parçaları liste öğeleri olarak döndürür. 

``` python
a = "Hello, World!"
print(a.split(",")) # returns ['Hello', ' World!']
``` 

### Python String Metodu 

Python’da stringler üzerinde kullanabileceğin hazır (built-in) metotlar vardır.  


### Python Dize (String) Metotları

| Metot | Açıklama |
| :--- | :--- |
| `capitalize()` | Dizinin ilk karakterini büyük harfe dönüştürür. |
| `casefold()` | Dizeyi küçük harfe dönüştürür (daha agresif küçültme). |
| `center()` | Dizeyi belirli bir uzunlukta ortalar. |
| `count()` | Dizede belirtilen değerin kaç kez geçtiğini döndürür. |
| `encode()` | Dizinin kodlanmış sürümünü döndürür. |
| `endswith()` | Dizinin belirtilen değerle bitip bitmediğini kontrol eder. |
| `expandtabs()` | Dizinin tab boyutunu ayarlar. |
| `find()` | Dizede belirtilen değeri arar ve bulunduğu konumu döndürür. |
| `format()` | Dizideki belirtilen değerleri biçimlendirir. |
| `format_map()` | Bir sözlükteki değerleri dizide biçimlendirir. |
| `index()` | Dizede belirtilen değeri arar ve bulunduğu konumu döndürür (bulunmazsa hata verir). |
| `isalnum()` | Tüm karakterler alfasayısal ise (`True`) döndürür. |
| `isalpha()` | Tüm karakterler alfabedeki harfler ise (`True`) döndürür. |
| `isascii()` | Tüm karakterler ASCII karakterler ise (`True`) döndürür. |
| `isdecimal()` | Tüm karakterler ondalık sayı ise (`True`) döndürür. |
| `isdigit()` | Tüm karakterler rakam ise (`True`) döndürür. |
| `isidentifier()` | Dize geçerli bir tanımlayıcı ise (`True`) döndürür. |
| `islower()` | Tüm harfler küçük harf ise (`True`) döndürür. |
| `isnumeric()` | Tüm karakterler sayısal ise (`True`) döndürür. |
| `isprintable()` | Tüm karakterler yazdırılabilir ise (`True`) döndürür. |
| `isspace()` | Tüm karakterler boşluk karakteri ise (`True`) döndürür. |
| `istitle()` | Dize başlık kurallarına uyuyorsa (`True`) döndürür. |
| `isupper()` | Tüm harfler büyük harf ise (`True`) döndürür. |
| `join()` | Bir yinelenebilir nesnenin öğelerini dizeye dönüştürür. |
| `ljust()` | Dizinin sola hizalanmış sürümünü döndürür. |
| `lower()` | Dizeyi küçük harfe dönüştürür. |
| `lstrip()` | Dizinin sol taraftaki 

### Python - String Birleştirme

İki string’i birleştirmek (concatenate) için + operatörünü kullanılır. 

``` python
a = "Hello"
b = "World"
c = a + b
print(c)
```  

İki string’in arasına boşluk eklemek için " " kullanabilirsin: 

``` python
a = "Hello"
b = "World"
c = a + " " + b
print(c)
```     
### Python String Formatlama 

stringler ile sayıları doğrudan birleştirilemez.

``` python
age = 36
#This will produce an error:
txt = "My name is John, I am " + age
print(txt)
```   

Ama f-string veya format() metodu kullanarak stringleri ve sayıları birleştirilebilir.

### F-Stringler 

F-String, Python 3.6’da tanıtıldı ve artık string formatlamada tercih edilen yöntemdir.

Bir string’i f-string olarak belirtmek için, string literal’in önüne f koyarsın ve küme parantez {} içinde değişkenler veya diğer işlemler için yer tutucular kullanılır. 

``` python
age = 36
txt = f"My name is John, I am {age}"
print(txt))
``` 

### Yer Tutucular ve Değiştiriciler 

Bir yer tutucu (placeholder), içine değişkenler, işlemler, fonksiyonlar ve değeri biçimlendirmek için kullanılan modifier’lar alabilir. 

``` python
price = 59
txt = f"The price is {price} dollars"
print(txt)
``` 

Bir yer tutucu (placeholder), değeri biçimlendirmek için bir modifier içerebilir.

Bir modifier eklemek için, yer tutucunun içine bir iki nokta : koyar ve ardından geçerli bir format tipi yazılır. Örneğin, .2f → 2 ondalık basamaklı sabit noktalı sayı anlamına gelir.

``` python
price = 59
txt = f"The price is {price:.2f} dollars"
print(txt)
```   

Bir yer tutucu (placeholder), içine Python kodu da alabilir; örneğin matematiksel işlemler gibi:    

``` python
txt = f"The price is {20 * 59} dollars"
print(txt)
```   

### Python Kaçış Karakterleri 

Bir string içinde geçersiz karakterler eklemek için escape karakteri kullanılır.  

Escape karakteri, eklemek istediğiniz karakterin önüne ters eğik çizgi \ konularak kullanılır.  

Örneğin, çift tırnaklarla çevrili bir string içinde çift tırnak kullanmak geçersizdir:  

``` python
txt = "We are the so-called "Vikings" from the north."
```   

Bu sorunu çözmek için escape karakteri \" kullanılır: 

Escape karakteri, normalde izin verilmeyen durumlarda çift tırnakları string içinde kullanmanıza olanak tanır. 

``` python
txt = "We are the so-called \"Vikings\" from the north."  
```   
Python’da kullanılan diğer escape karakterleri şunlardır: 

## 📝 Python Kaçış Karakterleri (Escape Characters)

| Kod | Sonuç |
| :--- | :--- |
| `\'` | Single Quote (Tek Tırnak) |
| `\\` | Backslash (Ters Eğik Çizgi) |
| `\n` | New Line (Yeni Satır) |
| `\r` | Carriage Return (Satır Başı) |
| `\t` | Tab (Sekme) |
| `\b` | Backspace (Geri Boşluk) |
| `\f` | Form Feed (Sayfa Atlatma) |
| `\ooo` | Octal value (Sekizlik Değer) |
| `\xhh` | Hex value (Onaltılık Değer) | 

## Python Boolean'lar 

Boolean'lar, iki değerden birini temsil eder: True veya False. 

### Boolean Değerleri 

Programlamada genellikle bir ifadenin True mu yoksa False mu olduğunu bilmen gerekir. 

Python’da herhangi bir ifadeyi değerlendirebilir ve iki olası cevaptan birini verir: True veya False. 

İki değeri karşılaştırdığında, ifade değerlendirilir ve Python Boolean bir cevap döndürür: 

``` python
print(10 > 9)
print(10 == 9)
print(10 < 9)
``` 

Bir if ifadesinde bir koşulu çalıştırdığında, Python True veya False döndürür. 

``` python
a = 200
b = 33

if b > a:
  print("b is greater than a")
else:
  print("b is not greater than a")
```  

### Değerleri ve değişkenleri değerlendirmek. 

bool() fonksiyonu, herhangi bir değeri değerlendirmeni sağlar ve sonucunda sana True veya False döndürür. 

``` python
print(bool("Hello"))
print(bool(15))
``` 

``` python
x = "Hello"
y = 15

print(bool(x))
print(bool(y))
```  
### Çoğu değer True 

Neredeyse tüm değerler, bir tür içerik taşıyorsa True olarak değerlendirilir.

Herhangi bir string True’dur, yalnızca boş string ("") False’tur.

Herhangi bir sayı True’dur, yalnızca 0 False’tur. 

Herhangi bir liste, tuple, set veya sözlük True’dur, yalnızca boş olanlar False’tur. 

``` python
bool("abc")
bool(123)
bool(["apple", "cherry", "banana"]
``` 

### Bazı değerler False 

Aslında False olarak değerlendirilen çok fazla değer yoktur; yalnızca boş değerler ((), [], {}, ""), sayı 0, ve None değeri False olarak değerlendirilir. Tabii ki, False değeri de False olarak değerlendirilir. 

``` python
bool(False)
bool(None)
bool(0)
bool("")
bool(())
bool([])
bool({})
``` 

Bir değer veya nesne daha False olarak değerlendirilir: Eğer bir sınıftan oluşturulmuş bir nesne varsa ve bu sınıfın _len_ fonksiyonu 0 veya False döndürüyorsa, o nesne False kabul edilir. 

``` python
class myclass():
  def __len__(self):
    return 0

myobj = myclass()
print(bool(myobj))
```    

### Fonksiyonlar Boolean değer döndürebilir. 

Boolean değer döndüren fonksiyonlar oluşturulabilir. 

``` python
def myFunction() :
  return True

print(myFunction())
```   

Bir fonksiyonun döndürdüğü Boolean sonuca bağlı olarak kod çalıştırabilirsin. 

``` python
def myFunction() :
  return True

if myFunction():
  print("YES!")
else:
  print("NO!")
```  

Python ayrıca Boolean değer döndüren birçok yerleşik (built-in) fonksiyona sahiptir. Örneğin, isinstance() fonksiyonu bir nesnenin belirli bir veri tipi olup olmadığını kontrol etmek için kullanılabilir. 

``` python
x = 200
print(isinstance(x, int))
```  

## Python Operatörler

Operatörler, değişkenler ve değerler üzerinde işlemler gerçekleştirmek için kullanılır.

Aşağıdaki örnekte, iki değeri birbirine eklemek için + operatörünü kullanıyoruz:  

``` python
print(10 + 5)
```  
"+" operatörü genellikle iki değeri birbirine eklemek için kullanılsa da, yukarıdaki örnekte olduğu gibi, bir değişken ile bir değeri ya da iki değişkeni birbirine eklemek için de kullanılabilir: 


``` python
sum1 = 100 + 50      # 150 (100 + 50)
sum2 = sum1 + 250    # 400 (150 + 250)
sum3 = sum2 + sum2   # 800 (400 + 400))
```  

### Python Aritmetik Operatörler 
Farklı aritmetik operatörlerin kullanıldığı bir örnek aşağıda verilmiştir: 


Aritmetik operatörler, sayısal değerlerle birlikte yaygın matematiksel işlemleri gerçekleştirmek için kullanılır: 

| Operatör | Adı | Örnek |
| :---: | :--- | :---: |
| **+** | Toplama | `x + y` |
| **-** | Çıkarma | `x - y` |
| **\*** | Çarpma | `x * y` |
| **/** | Bölme | `x / y` |
| **%** | Modülüs | `x % y` |
| **\*\*** | Üs Alma (Kuvvet) | `x ** y` |
| **//** | Tam Sayı Bölmesi (Floor division) | `x // y` | 

Farklı aritmetik operatörlerin kullanıldığı bir örnek aşağıda verilmiştir: 

``` python
x = 15
y = 4

print(x + y)
print(x - y)
print(x * y)
print(x / y)
print(x % y)
print(x ** y)
print(x // y)
```  

#### Python Bölme İşlemi 

Python’da iki tane bölme operatörü vardır:

+ / – Bölme (float bir değer döndürür)
+ // – Taban bölme (integer bir değer döndürür) 

``` python
#bölme işlemleri her zaman float döndürür
x = 12
y = 5

print(x / y)
```  

``` python
# Taban bölme (floor division) her zaman bir integer değer döndürür.

# Bu işlem, sonucu en yakın aşağıdaki tam sayıya yuvarlar:

x = 12
y = 5

print(x // y)
```  

### Python Atama Operatörleri 

Değerleri değişkenleri atamak için kulllanılırlar: 

| Operatör | Örnek (Example) | Aynı Anlama Gelir (Same As) |
| :---: | :--- | :--- |
| **=** | `x = 5` | `x = 5` |
| **+=** | `x += 3` | `x = x + 3` |
| **-=** | `x -= 3` | `x = x - 3` |
| **\*=** | `x *= 3` | `x = x * 3` |
| **\/**= | `x /= 3` | `x = x / 3` |
| **%=** | `x %= 3` | `x = x % 3` |
| **\/\/**= | `x //= 3` | `x = x // 3` |
| **\*\*=** | `x **= 3` | `x = x ** 3` |
| **&=** | `x &= 3` | `x = x & 3` |
| **\|=** | `x \|= 3` | `x = x \| 3` |
| **^=** | `x ^= 3` | `x = x ^ 3` |
| **>>=** | `x >>= 3` | `x = x >> 3` |
| **<<=** | `x <<= 3` | `x = x << 3` |
| **:=** | `print(x := 3)` | `x = 3; print(x)` | 

### Python Karşılaştırma Operatörleri 


| Operatör | Adı (Name) | Örnek (Example) |
| :---: | :--- | :---: |
| **==** | Eşittir (Equal) | `x == y` |
| **!=** | Eşit Değildir (Not equal) | `x != y` |
| **>** | Büyüktür (Greater than) | `x > y` |
| **<** | Küçüktür (Less than) | `x < y` |
| **>=** | Büyük veya Eşittir (Greater than or equal to) | `x >= y` |
| **<=** | Küçük veya Eşittir (Less than or equal to) | `x <= y` | 


Karşılaştırma operatörleri, yapılan karşılaştırmaya bağlı olarak True veya False değerini döndürür: 

``` python
x = 5
y = 3

print(x == y)
print(x != y)
print(x > y)
print(x < y)
print(x >= y)
print(x <= y)
``` 

Python, karşılaştırma operatörlerini zincirleme kullanmaya olanak tanır: 

``` python
x = 5

print(1 < x < 10)

print(1 < x and x < 10)y)
```   

### Python Mantıksal Operatörler 

Mantıksal operatörler, koşullu ifadeleri birleştirmek için kullanılır: 


| Operatör | Açıklama (Description) | Örnek (Example) |
| :---: | :--- | :--- |
| **and** | Her iki ifade de doğruysa (True) doğru döndürür. | `x < 5 and x < 10` |
| **or** | İfadelerden en az biri doğruysa (True) doğru döndürür. | `x < 5 or x < 4` |
| **not** | Sonucu tersine çevirir; sonuç doğruysa (True) yanlış (False) döndürür. | `not(x < 5 and x < 10)` | 

Bir sayının 0’dan büyük ve 10’dan küçük olup olmadığını test etme: 

``` python
x = 5

print(x > 0 and x < 10)
```    

Bir sayının 5'ten küçük veya 10'dan büüyk olduğunu test etme: 

``` python
x = 5

print(x < 5 or x > 10)
```  
**not** ile sonuç terse çevrilir: 

``` python
x = 5

print(not(x > 3 and x < 10))
# Çıktı False
```  

### Python Kimlik (Identity) Operatörleri 

Kimlik operatörleri, nesnelerin eşit olup olmadığını değil, gerçekten aynı nesne olup olmadıklarını ve aynı bellek konumunu paylaşıp paylaşmadıklarını kontrol etmek için kullanılır: 


| Operatör | Açıklama (Description) | Örnek (Example) |
| :---: | :--- | :---: |
| **is** | Her iki değişken de aynı nesne ise Doğru (True) döndürür. | `x is y` |
| **is not** | Her iki değişken de aynı nesne değilse Doğru (True) döndürür. | `x is not y` | 


**is** operatörü, iki değişken aynı nesneye işaret ediyorsa True değerini döndürür: 

``` python
x = ["apple", "banana"]
y = ["apple", "banana"]
z = x

print(x is z)
print(x is y)
print(x == y)
# #Çıktı: 
# #True 
# False 
# True
```   
is not operatörü, iki değişken aynı nesneye işaret etmiyorsa True değerini döndürür: 


``` python
x = ["apple", "banana"]
y = ["apple", "banana"]

print(x is not y)
```   

**is** ve **==** arasındaki fark: 

+ is – Her iki değişkenin bellekte aynı nesneye işaret edip etmediğini kontrol eder

+ == – Her iki değişkenin değerlerinin eşit olup olmadığını kontrol eder 

``` python
x = [1, 2, 3]
y = [1, 2, 3]

print(x == y)
print(x is y)
#Çıktı: 
#True 
#False
```  
### Python Üyelik (Membership) Operatörleri

Üyelik operatörleri, bir veri yapısında belirli bir öğenin bulunup bulunmadığını test etmek için kullanılır: 

| Operatör | Açıklama (Description) | Örnek (Example) |
| :---: | :--- | :---: |
| **in** | Belirtilen değere sahip bir sıra (sequence) nesne içinde mevcutsa Doğru (True) döndürür. | `x in y` |
| **not in** | Belirtilen değere sahip bir sıra nesne içinde mevcut değilse Doğru (True) döndürür. | `x not in y` |


``` python
fruits = ["apple", "banana", "cherry"]

print("banana" in fruits)
``` 

``` python
fruits = ["apple", "banana", "cherry"]

print("pineapple" not in fruits)
```  

Üyelik operatörleri, string (metin) veri tipleri ile de çalışır: 

``` python
text = "Hello World"

print("H" in text)
print("hello" in text)
print("z" not in text)
```  

### Python Bit Düzeyinde (Bitwise) Operatörler 

Bit düzeyinde operatörler, (ikili) sayıları karşılaştırmak ve üzerinde işlem yapmak için kullanılır: 

| Operatör | Adı (Name) | Açıklama (Description) | Örnek (Example) |
| :---: | :--- | :--- | :---: |
| **&** | AND | Her iki bit de 1 ise ilgili biti 1 yapar. | `x & y` |
| **\|** | OR | Bitlerden biri 1 ise ilgili biti 1 yapar. | `x \| y` |
| **^** | XOR | Yalnızca bir bit 1 ise ilgili biti 1 yapar. | `x ^ y` |
| **~** | NOT | Tüm bitleri tersine çevirir. | `~x` |
| **<<** | Zero fill left shift | Sağdan sıfırlar iterek kaydırma yapar ve en soldaki bitlerin düşmesine izin verir. | `x << 2` |
| **>>** | Signed right shift | Soldan en soldaki bitin kopyalarını iterek kaydırma yapar ve en sağdaki bitlerin düşmesine izin verir. | `x >> 2` |

*** 
& operatörü, her bir biti karşılaştırır ve her ikisi de 1 ise 1, aksi halde 0 olarak ayarlar 

``` python
print(6 & 3)
``` 
6’nın ikili (binary) gösterimi: 0110
3’ün ikili gösterimi: 0011

& operatörü bitleri karşılaştırır
Sonuç: 0010, yani decimal olarak 2. 

*** 
"
**|**" operatörü, her bir biti karşılaştırır ve biri veya her ikisi 1 ise 1, aksi halde 0 olarak ayarlar:  
``` python
print(6 | 3)
``` 

6’nın ikili (binary) gösterimi: 0110
3’ün ikili gösterimi: 0011

"**|**" operatörü bitleri karşılaştırır:   
Sonuç: 0111, yani decimal olarak 7.

*** 
^ operatörü, her bir biti karşılaştırır ve sadece biri 1 ise 1, aksi halde (her ikisi de 1 veya her ikisi de 0 ise) 0 olarak ayarlar: 

``` python
print(6 ^ 3)
```  

6’nın ikili (binary) gösterimi: 0110
3’ün ikili gösterimi: 0011

^ operatörü bitleri karşılaştırır:  
Sonuç: 0101, yani decimal olarak 5. 

### Python Operatör Önceliği (Operator Precedence) 

Operatör önceliği, işlemlerin hangi sırayla gerçekleştirileceğini belirler. 

Parantezlerin en yüksek önceliğe sahiptir, yani parantez içindeki ifadeler önce değerlendirilir: 

``` python
print((6 + 3) - (6 + 3)) 
# Çıktı = 0 
``` 

Çarpma (*), toplama (+) işleminden daha yüksek önceliğe sahiptir; bu nedenle çarpma işlemleri, toplama işlemlerinden önce değerlendirilir: 

``` python
print(100 + 5 * 3)
# Çıktı = 115
```   
#### Öncelik Sırası 
Öncelik sırası aşağıdaki tabloda gösterilmiştir; en yüksek öncelik tablonun en üstünde yer alıyor:
 

| Operatör | Açıklama (Description) |
| :---: | :--- |
| **()** | Parantezler (Parentheses) |
| **\*\*** | Üs Alma (Exponentiation) |
| **+x, -x, ~x** | Tekil Artı, Tekil Eksi ve Bit Düzeyi NOT (Unary plus, unary minus, and bitwise NOT) |
| **\*, /, //, %** | Çarpma, Bölme, Tam Sayı Bölmesi ve Modülüs (Multiplication, division, floor division, and modulus) |
| **+, -** | Toplama ve Çıkarma (Addition and subtraction) |
| **<<, >>** | Bit Düzeyi Sol ve Sağ Kaydırmalar (Bitwise left and right shifts) |
| **&** | Bit Düzeyi AND |
| **^** | Bit Düzeyi XOR |
| **\|** | Bit Düzeyi OR |
| **==, !=, >, >=, <, <=, is, is not, in, not in** | Karşılaştırmalar, Kimlik ve Üyelik Operatörleri (Comparisons, identity, and membership operators) |
| **not** | Mantıksal NOT (Logical NOT) |
| **and** | Mantıksal AND |
| **or** | Mantıksal OR | 


### Soldan Sağa Değerlendirme (Left-to-Right Evaluation) 

Eğer iki operatörün önceliği aynıysa, ifade soldan sağa doğru değerlendirilir.  

``` python
print(5 + 4 - 7 + 3)
#Çıktı 5 
``` 
## Python Listeler 

``` python
mylist = ["apple", "banana", "cherry"]
```
Listeler, birden fazla öğeyi tek bir değişkende saklamak için kullanılır.

Listeler, Python’daki 4 yerleşik veri tipinden biridir ve veri koleksiyonlarını saklamak için kullanılır; diğer üçü Tuple, Set ve Dictionary olup, her birinin farklı özellikleri ve kullanım şekilleri vardır.

Listeler, köşeli parantezler kullanılarak oluşturulur: 

``` python
thislist = ["apple", "banana", "cherry"]
print(thislist)
``` 

#### Liste Ögeleri 
Liste öğeleri sıralıdır, değiştirilebilir ve tekrarlayan değerleri kabul eder. Liste öğeleri indekslenmiştir: İlk öğenin indeksi [0] İkinci öğenin indeksi [1]. Bu şekilde devam eder.

#### Sıralı 
Listelerin sıralı olduğunu söylediğimizde, öğelerin belirli bir sıraya sahip olduğunu ve bu sıranın değişmeyeceğini kastediyoruz.

Listeye yeni öğe eklediğinizde, bu öğeler listenin sonuna eklenir. 

Not: Bazı liste metodları öğelerin sırasını değiştirebilir, ancak genel olarak liste öğelerinin sırası değişmez. 

#### Değiştirilebilinir
Liste değiştirilebilir (changeable) özelliktedir; yani bir liste oluşturulduktan sonra öğeleri değiştirebilir, ekleyebilir ve silebiliriz. 

### Tekrarlayan Öğelere İzin Verir

Listeler indeksli olduklarından, aynı değere sahip öğelere sahip olabilirler: 

``` python
thislist = ["apple", "banana", "cherry", "apple", "cherry"]
print(thislist)
``` 
### Listenin Uzunluğu 
Bir listenin kaç öğe içerdiğini öğrenmek için len() fonksiyonu kullanılır: 

``` python
thislist = ["apple", "banana", "cherry"]
print(len(thislist))
``` 

### Liste Öğeleri – Veri Tipleri 
Liste öğeleri herhangi bir veri tipinde olabilir: 

``` python
list1 = ["apple", "banana", "cherry"] #string
list2 = [1, 5, 7, 9, 3] #integer
list3 = [True, False, False] #boolean 
``` 

Bir liste, farklı veri tiplerini bir arada içerebilir: 

``` python
list1 = ["abc", 34, True, 40, "male"]
```  

### type() 

Python açısından listeler, veri tipi 'list' olan nesneler (objects) olarak tanımlanır: 

``` python
<class 'list'>
``` 

``` python
mylist = ["apple", "banana", "cherry"]
print(type(mylist))
``` 

### list() Yapıcı (Constructor) 

Yeni bir liste oluştururken list() yapıcısı da kullanılabilir: 

``` python
thislist = list(("apple", "banana", "cherry")) # note the double round-brackets
print(thislist)
``` 

### Python – Liste Öğelerine Erişim

Liste öğeleri indekslenmiştir ve indeks numarasını kullanarak erişilebilir: 

``` python
thislist = ["apple", "banana", "cherry"]
print(thislist[1])
```  

Not: Listenin ilk öğesinin indeksi 0’dır. 

**Negatif İndeksleme** 

Negatif indeksleme, listenin sonundan başlamak anlamına gelir: -1 → son öğe -2 → sondan ikinci öğe. Bu şekilde devam eder. 

``` python
thislist = ["apple", "banana", "cherry"]
print(thislist[-1]) 
#Çıktı cherry
``` 
**İndeks Aralığı (Range of Indexes)**  
Başlangıç ve bitiş indekslerini belirterek bir indeks aralığı seçebilirsiniz.

Bir aralık belirtildiğinde, döndürülen değer belirtilen öğeleri içeren yeni bir liste olur. 

``` python
thislist = ["apple", "banana", "cherry", "orange", "kiwi", "melon", "mango"]
print(thislist[2:5])
```  

Not: Arama, 2. indeksten (dahil) başlayacak ve 5. indekste (dahil değil) sona erecektir. 

Başlangıç değeri belirtilmezse, aralık listenin ilk öğesinden başlar. 

``` python
thislist = ["apple", "banana", "cherry", "orange", "kiwi", "melon", "mango"]
print(thislist[:4]))
``` 
Bitiş değeri belirtilmezse, aralık listenin sonuna kadar devam eder. 

``` python
thislist = ["apple", "banana", "cherry", "orange", "kiwi", "melon", "mango"]
print(thislist[2:])
``` 

**Negatif İndeks Aralığı**  
Aramaya listenin sonundan başlanılıcaksa, negatif indeksler kullanılanilir:

``` python
thislist = ["apple", "banana", "cherry", "orange", "kiwi", "melon", "mango"]
print(thislist[-4:-1])
``` 

**Öğe Var mı Kontrol Etme**  

Belirli bir öğenin listede olup olmadığını kontrol etmek için in anahtar kelimesi kullanılır:  

``` python
thislist = ["apple", "banana", "cherry"]
if "apple" in thislist:
  print("Yes, 'apple' is in the fruits list")
```  
### Liste Öğelerini Değiştirme  

#### Öğe Değerini Değiştirme

Belirli bir öğenin değerini değiştirmek için, indeks numarasına başvurulur: 

``` python
thislist = ["apple", "banana", "cherry"]
thislist[1] = "blackcurrant"
print(thislist)
```  

#### Belirli Bir Aralıktaki Öğelerin Değerini Değiştirme  
Belirli bir aralıktaki öğelerin değerini değiştirmek için: Yeni değerleri içeren bir liste oluşturup yeni değerlerin ekleneceği indeks aralığını belirtmek lazım. 

``` python
thislist = ["apple", "banana", "cherry", "orange", "kiwi", "mango"]
thislist[1:3] = ["blackcurrant", "watermelon"]
print(thislist)
``` 

Eğer yerine koyulan ögeden daha fazla öğe eklenilirse, yeni öğeler belirtilen konuma eklenir ve geri kalan öğeler buna göre kaydırılır. 

``` python
thislist = ["apple", "banana", "cherry"]
thislist[1:2] = ["blackcurrant", "watermelon"]
print(thislist)
#Çıktı 
# ['apple', 'blackcurrant', 'watermelon', 'cherry']
```  
Not: Eklenen öğe sayısı, yerine koyulan öğe sayısıyla eşleşmediğinde, listenin uzunluğu değişir.

Eğer yerine koyduğunuzdan daha az öğe eklerseniz, yeni öğeler belirtilen konuma eklenir ve geri kalan öğeler buna göre kaydırılır. 

``` python
thislist = ["apple", "banana", "cherry"]
thislist[1:3] = ["watermelon"]
print(thislist)
# Çıktı 
# ['apple', 'watermelon']
```

### Python – Listeye Öğeler Ekleme

Listenin sonuna bir öğe eklemek için append() metodunu kullanın: 

``` python
thislist = ["apple", "banana", "cherry"]
thislist.append("orange")
print(thislist)
```

Mevcut değerleri değiştirmeden yeni bir öğe eklemek için insert() metodunu kullanabiliriz.

insert() metodu, öğeyi belirtilen indekse ekler: 

``` python
thislist = ["apple", "banana", "cherry"]
thislist.insert(1, "orange")
print(thislist)
```
Not: Yukarıdaki örneklerin sonucunda, listeler artık 4 öğe içerecektir.

### Listeyi Genişletme (Extend List) 

Mevcut listeye başka bir listenin öğelerini eklemek için extend() metodunu kullanın. 

``` python
thislist = ["apple", "banana", "cherry"]
tropical = ["mango", "pineapple", "papaya"]
thislist.extend(tropical)
print(thislist)
``` 

Öğeler, listenin sonuna eklenecektir.

### Herhangi Bir İterable Öğeyi Ekleme
extend() metodu sadece listeleri eklemekle sınırlı değildir; tuple, set, dictionary gibi herhangi bir iterable nesneyi de ekleyebilirsiniz. 

``` python
thislist = ["apple", "banana", "cherry"]
thistuple = ("kiwi", "orange")
thislist.extend(thistuple)
print(thislist)
```  

### Python – Liste Öğelerini Kaldırma

#### Belirli Öğeyi Kaldırma (Remove Specified Item)

remove() metodu, belirtilen öğeyi listeden siler. 

``` python
thislist = ["apple", "banana", "cherry"]
thislist.remove("banana")
print(thislist)
``` 

Eğer belirtilen değere sahip birden fazla öğe varsa, remove() metodu ilk karşılaşılan öğeyi siler. 

``` python
thislist = ["apple", "banana", "cherry", "banana", "kiwi"]
thislist.remove("banana")
print(thislist)
```   

#### Belirli İndeksi Kaldırma  

pop() metodu, belirtilen indeksteki öğeyi listeden çıkarır.

``` python
thislist = ["apple", "banana", "cherry"]
thislist.pop(1)
print(thislist)
``` 

Eğer indeks belirtilmezse, pop() metodu listenin son öğesini kaldırır.


``` python
thislist = ["apple", "banana", "cherry"]
thislist.pop()
print(thislist)
```  
del anahtar kelimesi de belirtilen indeksteki öğeyi kaldırır:

``` python
thislist = ["apple", "banana", "cherry"]
del thislist[0]
print(thislist)
```  

del anahtar kelimesi, bir listeyi tamamen silmek için de kullanılabilir. 

``` python
thislist = ["apple", "banana", "cherry"]
del thislist
```   

#### Listeyi Temizleme

clear() metodu, listenin tüm öğelerini siler. Liste hala var olur, ancak artık içerik içermez.

``` python
thislist = ["apple", "banana", "cherry"]
thislist.clear()
print(thislist)
```    

### Python – Listelerde Döngü

### Bir Liste Üzerinde Döngü


Liste öğeleri üzerinde for döngüsü kullanarak dönebilirsiniz:

``` python
thislist = ["apple", "banana", "cherry"]
for x in thislist:
  print(x)
```  

### İndeks Numaraları Üzerinden Döngü 

Liste öğelerine indeks numaralarıyla de erişip dönebilirsiniz. Bunun için range() ve len() fonksiyonlarını kullanarak uygun bir iterable oluşturun: 

``` python
thislist = ["apple", "banana", "cherry"]
for i in range(len(thislist)):
  print(thislist[i])
```  
Yukarıdaki örnekte oluşturulan iterable şudur: [0, 1, 2]. 

### While Döngüsü Kullanarak
Liste öğeleri üzerinde while döngüsü kullanarak dönebilirsiniz.

len() fonksiyonu ile listenin uzunluğunu belirleyin.

İndeksi 0’dan başlatın ve indeksleri kullanarak liste öğeleri üzerinde döngü yapın.

Her iterasyondan sonra indeksi 1 artırmayı unutmayın.

``` python
thislist = ["apple", "banana", "cherry"]
i = 0
while i < len(thislist):
  print(thislist[i])
  i = i + 1
``` 

### Liste Anlaması Kullanarak Döngü 
List comprehension, listeler üzerinde döngü yapmak için en kısa ve özlü sözdizimini sunar: 

``` python
thislist = ["apple", "banana", "cherry"]
[print(x) for x in thislist]
```  

### Python – Liste Anlaması (List Comprehension) 

### List Comprehension
List comprehension, mevcut bir listenin değerlerine dayanarak yeni bir liste oluşturmak istediğinizde daha kısa bir sözdizimi sunar.

Örnek:

Bir meyve listesine dayanarak, adında "a" harfi geçen meyveleri içeren yeni bir liste oluşturmak istiyorsunuz.

List comprehension kullanmadan, içinde bir koşullu test bulunan bir for ifadesi yazmanız gerekir:

``` python
fruits = ["apple", "banana", "cherry", "kiwi", "mango"]
newlist = []

for x in fruits:
  if "a" in x:
    newlist.append(x)

print(newlist)
```   

List comprehension ile tüm bunları yalnızca tek bir satır kod ile yapabilirsiniz: 

``` python
fruits = ["apple", "banana", "cherry", "kiwi", "mango"]

newlist = [x for x in fruits if "a" in x]

print(newlist)
``` 

### Syntax 

``` python
newlist = [expression for item in iterable if condition == True]
```  

Dönüş değeri yeni bir listedir; eski liste değişmeden kalır. 

**Koşul**  
Koşul, yalnızca True olarak değerlendirilen öğeleri kabul eden bir filtre gibidir.

``` python
# Sadece "apple" olmayan öğeleri kabul et:
newlist = [x for x in fruits if x != "apple"]
```  

if x != "apple" koşulu, "apple" dışında tüm öğeler için True döner ve yeni listenin "apple" hariç tüm meyveleri içermesini sağlar.

Koşul isteğe bağlıdır ve atlanabilir: 

``` python
newlist = [x for x in fruits]
```  
**Iterable**
Iterable, bir liste, tuple, set gibi herhangi bir iterable nesne olabilir. 

Bir iterable oluşturmak için range() fonksiyonunu kullanılabilir.

``` python
newlist = [x for x in range(10)]
```  
Aynı örnek, ancak bir koşul ile:

``` python
newlist = [x for x in range(10) if x < 5]
``` 

#### İfade (Expression)
İfade, iterasyon sırasında mevcut öğedir, ancak aynı zamanda sonuçtur; yeni listedeki öğe hâline gelmeden önce üzerinde işlem yapabilirsiniz.

``` python
newlist = [x.upper() for x in fruits]
```  

Sonucu istediğiniz gibi ayarlayabilirsiniz: 

``` python
newlist = ['hello' for x in fruits]
``` 

İfade, bir filtre gibi değil, sonucu manipüle etmenin bir yolu olarak da koşullar içerebilir: 

``` python
newlist = [x if x != "banana" else "orange" for x in fruits]
```  

Yukarıdaki örnekteki ifade şöyle der:

"Öğe banana değilse onu döndür, eğer banana ise orange döndür." 

### Python - Listeleri Sıralama 

### Listeyi Alfabetik ve Sayısal Olarak Sıralama  

Liste nesnelerinin, listeyi alfanumerik olarak, varsayılan olarak artan şekilde sıralayan bir sort() metodu vardır: 

``` python
thislist = ["orange", "mango", "kiwi", "pineapple", "banana"]
thislist.sort()
print(thislist)
``` 
Listeyi sayısal olarak sıralama:

``` python
thislist = [100, 50, 65, 82, 23]
thislist.sort()
print(thislist)
```  

### Azalan Sırada Sıralama

Azalan sırada sıralamak için reverse=True anahtar kelime argümanını kullanılmalı: 

``` python
thislist = ["orange", "mango", "kiwi", "pineapple", "banana"]
thislist.sort(reverse = True)
print(thislist)
``` 

``` python
thislist = [100, 50, 65, 82, 23]
thislist.sort(reverse = True)
print(thislist)
```  

### Sıralama Fonksiyonunu Özelleştirme

Kendi fonksiyonunuzu, key=function anahtar kelime argümanını kullanarak da özelleştirebilirsiniz.

Fonksiyon, listenin sıralanmasında kullanılacak bir sayı döndürecektir (en düşük sayı önce gelir):


``` python
def myfunc(n):
  return abs(n - 50)

thislist = [100, 50, 65, 82, 23]
thislist.sort(key = myfunc)
print(thislist)
```  

### Büyük/Küçük Harf Duyarsız Sıralama

Varsayılan olarak, sort() metodu büyük/küçük harf duyarlıdır, bu da tüm büyük harflerin küçük harflerden önce sıralanmasına neden olur: 

``` python
thislist = ["banana", "Orange", "Kiwi", "cherry"]
thislist.sort()
print(thislist)
``` 

Neyse ki, bir listeyi sıralarken yerleşik fonksiyonları (built-in functions) key fonksiyonu olarak kullanabiliriz.

Yani, büyük/küçük harf duyarsız bir sıralama yapmak istiyorsanız, str.lower’ı key fonksiyonu olarak kullanın: 

``` python
thislist = ["banana", "Orange", "Kiwi", "cherry"]
thislist.sort(key = str.lower)
print(thislist)
```  

### Ters Sıralama
Peki ya bir listenin sırasını, alfabetik sıraya bakılmaksızın ters çevirmek istiyorsanız?

reverse() metodu, öğelerin mevcut sıralama düzenini tersine çevirir. 

``` python
thislist = ["banana", "Orange", "Kiwi", "cherry"]
thislist.reverse()
print(thislist)
``` 

### Python - Listeleri Kopyalama

**Bir Listeyi Kopyalama**  

Sadece list2 = list1 yazarak bir listeyi kopyalayamazsınız, çünkü: list2 yalnızca list1’in bir referansı olur ve list1’de yapılan değişiklikler otomatik olarak list2’yi de etkiler. 

#### copy() Metodunu Kullanma
Bir listeyi kopyalamak için yerleşik copy() metodunu kullanabilirsiniz.

``` python
thislist = ["apple", "banana", "cherry"]
mylist = thislist.copy()
print(mylist)
``` 

#### list() Metodunu Kullanma
Bir kopya oluşturmanın bir diğer yolu, yerleşik list() metodunu kullanmaktır.

``` python
thislist = ["apple", "banana", "cherry"]
mylist = list(thislist)
print(mylist)
```  

#### Slice Operatörünü Kullanma
Bir listeyi kopyalamanın bir diğer yolu da : (slice) operatörünü kullanmaktır.

``` python
thislist = ["apple", "banana", "cherry"]
mylist = thislist[:]
print(mylist)
``` 

### Python - Listeleri Birleştirme

#### İki Listeyi Birleştirme

Python’da iki veya daha fazla listeyi birleştirmenin (concatenate) birkaç yolu vardır.

Bunlardan en kolay yollarından biri + operatörünü kullanmaktır.


``` python
list1 = ["a", "b", "c"]
list2 = [1, 2, 3]

list3 = list1 + list2
print(list3)
``` 

İki listeyi birleştirmenin bir diğer yolu, list2’deki tüm öğeleri tek tek list1’e eklemektir (append): 


``` python
list1 = ["a", "b" , "c"]
list2 = [1, 2, 3]

for x in list2:
  list1.append(x)

print(list1)
```  

Veya extend() metodunu kullanabilirsiniz; amacı, bir listedeki öğeleri başka bir listeye eklemektir: 


``` python
list1 = ["a", "b" , "c"]
list2 = [1, 2, 3]

list1.extend(list2)
print(list1)
``` 

### List Metodları

| Metot (Method) | Açıklama (Description) |
| :---: | :--- |
| **append()** | Listenin sonuna bir öğe ekler. |
| **clear()** | Listedeki tüm öğeleri kaldırır. |
| **copy()** | Listenin bir kopyasını döndürür. |
| **count()** | Belirtilen değere sahip öğelerin sayısını döndürür. |
| **extend()** | Bir listenin (veya herhangi bir yinelenebilirin) öğelerini, mevcut listenin sonuna ekler. |
| **index()** | Belirtilen değere sahip ilk öğenin dizinini (index) döndürür. |
| **insert()** | Belirtilen konuma bir öğe ekler. |
| **pop()** | Belirtilen konumdaki öğeyi kaldırır. |
| **remove()** | Belirtilen değere sahip öğeyi kaldırır. |
| **reverse()** | Listenin sırasını tersine çevirir. |
| **sort()** | Listeyi sıralar. |

## Python Tuple'lar 

``` python
mytuple = ("apple", "banana", "cherry")
``` 

**Tuple**

Tuple’lar, bir değişkende birden fazla öğe saklamak için kullanılır.

Tuple, Python’da veri koleksiyonlarını saklamak için kullanılan 4 yerleşik (built-in) veri tipinden biridir; diğer üçü List, Set ve Dictionary’dir. Hepsinin farklı özellikleri ve kullanım şekilleri vardır.

Tuple, sıralı (ordered) ve değiştirilemez (unmodifiable/immutable) bir koleksiyondur.

Tuple’lar, yuvarlak parantez ( ) ile yazılır. 

``` python
thistuple = ("apple", "banana", "cherry")
print(thistuple)
``` 

**Tuple Öğeleri (Tuple Items)** 

Tuple öğeleri sıralıdır (ordered), değiştirilemez (unchangeable/immutable) ve tekrarlayan (duplicate) değerlere izin verir.

Tuple öğeleri indexlenmiştir; ilk öğenin index’i [0], ikinci öğenin index’i [1] vb. şeklindedir.


**Sıralı (Ordered)**

Tuple’ların sıralı olduğunu söylediğimizde, öğelerin belirli bir sıraya sahip olduğu ve bu sıranın değişmeyeceği anlamına gelir.

**Değiştirilemez (Unchangeable/Immutable)**

Tuple’lar değiştirilemezdir; yani tuple oluşturulduktan sonra öğeleri değiştiremez, ekleyemez veya silemezsiniz.

**Tekrarlara İzin Verir (Allow Duplicates)**

Tuple’lar indexli olduğu için, aynı değere sahip öğelere sahip olabilirler: 

``` python
thistuple = ("apple", "banana", "cherry", "apple", "cherry")
print(thistuple)
``` 

#### Tuple Uzunluğu (Tuple Length)

Bir tuple’ın kaç öğe içerdiğini öğrenmek için len() fonksiyonunu kullanın:

``` python
thistuple = ("apple", "banana", "cherry")
print(len(thistuple))
```  

### Tek Öğeli Tuple Oluşturma
Sadece bir öğe içeren bir tuple oluşturmak için, öğeden sonra virgül (,) eklenmesi gerekir; aksi takdirde Python bunu tuple olarak tanımaz.


``` python
thistuple = ("apple",)
print(type(thistuple))

#Tuple değil
thistuple = ("apple")
print(type(thistuple))
```  

### Tuple Öğeleri - Veri Tipleri

Tuple öğeleri herhangi bir veri tipinde olabilir: 

``` python
tuple1 = ("apple", "banana", "cherry")
tuple2 = (1, 5, 7, 9, 3)
tuple3 = (True, False, False)
```  

Bir tuple, farklı veri tiplerini içerebilir: 

``` python
tuple1 = ("abc", 34, True, 40, "male")
```   

**type()**

Python açısından, tuple’lar veri tipi 'tuple' olan nesneler (objects) olarak tanımlanır:

``` python
<class 'tuple'>
```  

``` python
mytuple = ("apple", "banana", "cherry")
print(type(mytuple))
```  

**tuple() Yapıcısı (Constructor)**

Bir tuple oluşturmak için tuple() yapıcısını kullanmak da mümkündür. 

``` python
thistuple = tuple(("apple", "banana", "cherry")) # note the double round-brackets
print(thistuple)
``` 

### Python - Tuple Öğelerine Erişim

Tuple öğelerine, köşeli parantezler içinde index numarasını belirterek erişilebilir:

``` python
thistuple = ("apple", "banana", "cherry")
print(thistuple[1])
```  

Not: İlk öğenin index’i 0’dır. 

### Negatif Indexleme (Negative Indexing)

Negatif indexleme, sondan başlamak anlamına gelir.

-1 son öğeye, -2 sondan ikinci öğeye vb. karşılık gelir.

``` python
thistuple = ("apple", "banana", "cherry")
print(thistuple[-1])
```   

### Index Aralığı (Range of Indexes)

Bir index aralığı belirtmek için, aralığın nereden başlayacağını ve nerede biteceğini belirtebilirsiniz.

Bir aralık belirtildiğinde, dönüş değeri belirtilen öğelerle yeni bir tuple olacaktır.


``` python
thistuple = ("apple", "banana", "cherry", "orange", "kiwi", "melon", "mango")
print(thistuple[2:5])
```  
Not: Arama, index 2’den (dahil) başlayacak ve index 5’te (dahil değil) sona erecektir. 

Başlangıç değerini atladığınızda, aralık ilk öğeden başlayacaktır: 


``` python
thistuple = ("apple", "banana", "cherry", "orange", "kiwi", "melon", "mango")
print(thistuple[:4])
``` 

Bitiş değerini atladığınızda, aralık tuple’ın sonuna kadar devam edecektir:  

``` python
thistuple = ("apple", "banana", "cherry", "orange", "kiwi", "melon", "mango")
print(thistuple[2:])
```  

### Negatif Index Aralığı
Aramaya tuple’ın sonundan başlamak istiyorsanız, negatif indexler belirtin: 

``` python
thistuple = ("apple", "banana", "cherry", "orange", "kiwi", "melon", "mango")
print(thistuple[-4:-1])
```   

### Öğenin Var Olup Olmadığını Kontrol Etme

Belirtilen bir öğenin tuple içinde olup olmadığını öğrenmek için in anahtar kelimesini kullanın:

``` python
thistuple = ("apple", "banana", "cherry")
if "apple" in thistuple:
  print("Yes, 'apple' is in the fruits tuple")
```  

### Python - Tuple’ları Güncelleme

Tuple’lar değiştirilemez (unchangeable/immutable); yani tuple oluşturulduktan sonra öğeleri değiştiremez, ekleyemez veya silemezsiniz.

Ama bazı çözümler (workarounds) vardır. 

### Tuple Değerlerini Değiştirme (Change Tuple Values)

Bir tuple oluşturulduktan sonra, değerlerini değiştiremezsiniz. Tuple’lar değiştirilemez (unchangeable/immutable) olarak da adlandırılır.

Ama bir çözüm vardır: Tuple’ı listeye dönüştürüp, liste üzerinde değişiklik yapabilir ve ardından listeyi tekrar tuple’a çevirebilirsiniz.

``` python
x = ("apple", "banana", "cherry")
y = list(x)
y[1] = "kiwi"
x = tuple(y)

print(x)
``` 

### Öğe Ekleme

Tuple’lar immutable (değiştirilemez) olduğundan, yerleşik bir append() metoduna sahip değillerdir, ama tuple’a öğe eklemenin başka yolları vardır.

1. Listeye dönüştürmek: Tuple’ı değiştirmek için kullanılan çözüm gibi, onu bir listeye dönüştürebilir, öğelerinizi ekleyebilir ve ardından listeyi tekrar tuple’a çevirebilirsiniz.

``` python
thistuple = ("apple", "banana", "cherry")
y = list(thistuple)
y.append("orange")
thistuple = tuple(y)
``` 

2. Tuple’ı tuple’a eklemek: Tuple’lara tuple eklemeye izin verilir. Yani, bir öğe (veya birden fazla öğe) eklemek istiyorsanız, öğe(leri) içeren yeni bir tuple oluşturun ve mevcut tuple’a ekleyin:

``` python
thistuple = ("apple", "banana", "cherry")
y = ("orange",)
thistuple += y

print(thistuple)
```  

Not: Sadece bir öğe içeren bir tuple oluştururken, öğeden sonra virgül (,) eklemeyi unutmayın; aksi takdirde tuple olarak tanınmaz. 

### Öğeleri Silme (Remove Items)

Not: Tuple’daki öğeler silinemez.

Tuple’lar değiştirilemez (unchangeable/immutable) olduğu için içindeki öğeleri silemezsiniz, ama tuple öğelerini değiştirmek ve eklemek için kullandığımız aynı çözümü (workaround) kullanabilirsiniz: 

``` python
thistuple = ("apple", "banana", "cherry")
y = list(thistuple)
y.remove("apple")
thistuple = tuple(y)
```  

Veya tuple’ı tamamen silebilirsiniz: 

``` python
thistuple = ("apple", "banana", "cherry")
del thistuple
print(thistuple)
```  

### Python - Tuple’ları Açma (Unpack Tuples)

Bir tuple oluşturduğumuzda, genellikle ona değerler atarız. Buna tuple’ı “paketlemek” (packing) denir: 

``` python
fruits = ("apple", "banana", "cherry")
```  

Ama Python’da, değerleri tekrar değişkenlere çıkarmamıza da izin verilir. Buna “unpacking” (paketten çıkarma) denir: 

``` python
fruits = ("apple", "banana", "cherry")

(green, yellow, red) = fruits

print(green)
print(yellow)
print(red)
```  

Not: Değişken sayısı, tuple’daki değer sayısı ile eşleşmelidir. Eşleşmezse, kalan değerleri bir liste olarak toplamak için yıldız işareti (*) kullanmalısınız. 

### Asterisk (*) Kullanımı

Değişken sayısı değer sayısından azsa, değişken isminin önüne * ekleyebilirsiniz; böylece kalan değerler liste olarak bu değişkene atanır:

``` python
fruits = ("apple", "banana", "cherry", "strawberry", "raspberry")

(green, yellow, *red) = fruits

print(green)
print(yellow)
print(red)
``` 

Asterisk işareti (*) son değişken dışında bir değişken adına eklenirse, Python kalan değerleri, kalan değişken sayısı ile eşleşene kadar bu değişkene atar.

``` python
fruits = ("apple", "mango", "papaya", "pineapple", "cherry")

(green, *tropic, red) = fruits

print(green)
print(tropic)
print(red)
```  

### Tuple’larda Döngü

Tuple öğeleri üzerinde, bir for döngüsü (for loop) kullanarak dönebilirsiniz: 

``` python
thistuple = ("apple", "banana", "cherry")
for x in thistuple:
  print(x)
```  

### Index Numaraları Üzerinden Döngü

Tuple öğeleri üzerinde, index numaralarını kullanarak da dönebilirsiniz.

Uygun bir iterable oluşturmak için range() ve len() fonksiyonlarını kullanın:

``` python
thistuple = ("apple", "banana", "cherry")
for i in range(len(thistuple)):
  print(thistuple[i])
``` 

### While Döngüsü Kullanarak
Tuple öğeleri üzerinde, bir while döngüsü (while loop) kullanarak dönebilirsiniz.

Tuple’ın uzunluğunu öğrenmek için len() fonksiyonunu kullanın, ardından 0’dan başlayarak indexleri kullanarak tuple öğeleri üzerinde döngü yapın.

Her iterasyondan sonra index’i 1 artırmayı unutmayın. 

``` python
thistuple = ("apple", "banana", "cherry")
i = 0
while i < len(thistuple):
  print(thistuple[i])
  i = i + 1
```  

### Python - Tuple’ları Birleştirme

#### İki Tuple’ı Birleştirme (Join Two Tuples)

İki veya daha fazla tuple’ı birleştirmek için + operatörünü kullanabilirsiniz:

``` python
tuple1 = ("a", "b" , "c")
tuple2 = (1, 2, 3)

tuple3 = tuple1 + tuple2
print(tuple3)
```   

#### Tuple’ları Çarpma

Bir tuple’ın içeriğini belirli bir sayı kadar tekrar etmek istiyorsanız, * operatörünü kullanılabilir.

``` python
fruits = ("apple", "banana", "cherry")
mytuple = fruits * 2

print(mytuple)
```  

### Tuple Metodları 

| Metot (Method) | Açıklama (Description) |
| :---: | :--- |
| **count()** | Belirtilen bir değerin demet içinde kaç kez geçtiğini döndürür. |
| **index()** | Demet içinde belirtilen bir değeri arar ve bulunduğu konumu (dizini) döndürür. |

## Python - Set’ler (Python Sets) 

``` python
myset = {"apple", "banana", "cherry"}
```  

### Set

Set’ler, birden fazla öğeyi tek bir değişkende saklamak için kullanılır.

Set, Python’da veri koleksiyonlarını saklamak için kullanılan 4 yerleşik veri tipinden biridir; diğerleri List, Tuple ve Dictionary olup, her birinin farklı özellikleri ve kullanımları vardır.

Bir set, sırasız (unordered), değiştirilemez (unchangeable)* ve indexlenemez (unindexed) bir koleksiyondur. 

Not: Set öğeleri değiştirilemez (unchangeable), ancak öğeleri silebilir ve yeni öğeler ekleyebilirsiniz. 

Set’ler, süslü parantezler {} ile yazılır. 

``` python
thisset = {"apple", "banana", "cherry"}
print(thisset)
```  

Not: Set’ler sırasız (unordered) olduğu için, öğelerin hangi sırayla görüneceğinden emin olunmalı:


#### Set Öğeleri (Set Items)

Set öğeleri sırasız (unordered), değiştirilemez (unchangeable) ve tekrarlanan değerleri içeremez.

#### Sırasız (Unordered)

Sırasız olması, set içindeki öğelerin belirli bir sırası olmadığı anlamına gelir.

Set öğeleri, her kullanımda farklı bir sırada görünebilir ve index veya key ile erişilemez.

#### Değiştirilemez (Unchangeable)

Set öğeleri değiştirilemez (unchangeable); yani bir set oluşturulduktan sonra öğeleri değiştiremezsiniz.

Bir set oluşturulduktan sonra, öğelerini değiştirilemez, ancak öğeleri silinebilir ve yeni öğeler eklenebilir.


#### Tekrarlar İzin Verilmez (Duplicates Not Allowed)

Set’lerde aynı değere sahip iki öğe olamaz.

``` python
thisset = {"apple", "banana", "cherry", "apple"}

print(thisset)
``` 

Not: Set’lerde True ve 1 aynı değer olarak kabul edilir ve tekrar (duplicate) olarak işlenir: 

``` python
thisset = {"apple", "banana", "cherry", True, 1, 2}

print(thisset)
```  

Not: Set’lerde False ve 0 aynı değer olarak kabul edilir ve tekrar (duplicate) olarak işlenir: 

``` python
thisset = {"apple", "banana", "cherry", False, True, 0}

print(thisset)
```  

**Bir Set’in Uzunluğunu Öğrenme (Get the Length of a Set)**

Bir set’in kaç öğe içerdiğini öğrenmek için len() fonksiyonunu kullanın.

``` python
thisset = {"apple", "banana", "cherry"}

print(len(thisset))
```  

#### Set Öğeleri - Veri Tipleri 

Set öğeleri herhangi bir veri tipi olabilir:

``` python
set1 = {"apple", "banana", "cherry"}
set2 = {1, 5, 7, 9, 3}
set3 = {True, False, False}
``` 

Bir set, farklı veri tiplerini içerebilir: 

``` python
set1 = {"abc", 34, True, 40, "male"}
``` 

**type()**
Python açısından, set’ler veri tipi 'set' olan objeler olarak tanımlanır:

``` python
<class 'set'>
```  

``` python
myset = {"apple", "banana", "cherry"}
print(type(myset))
```   

**set() Yapıcı Fonksiyonu**

Bir set oluşturmak için set() yapıcı fonksiyonunu da kullanabilirsiniz.

``` python
thisset = set(("apple", "banana", "cherry")) # note the double round-brackets
print(thisset)
``` 

### Python - Set Öğelerine Erişim

``` python
# Set üzerinde döngü yapma ve değerleri yazdırma:
thisset = {"apple", "banana", "cherry"}

for x in thisset:
  print(x)
```  

``` python 
# Set içinde "banana" olup olmadığını kontrol etma
thisset = {"apple", "banana", "cherry"}

print("banana" not in thisset)
```  

### Python - Set Öğeleri Ekleme

Bir set oluşturulduktan sonra, öğelerini değiştiremezsiniz, ancak yeni öğeler ekleyebilirsiniz.

Bir set’e tek bir öğe eklemek için add() metodunu kullanın. 

``` python 
thisset = {"apple", "banana", "cherry"}

thisset.add("orange")

print(thisset)
```  

#### Set Ekleme

Başka bir set’in öğelerini mevcut set’e eklemek için update() metodunu kullanın.

``` python 
thisset = {"apple", "banana", "cherry"}
tropical = {"pineapple", "mango", "papaya"}

thisset.update(tropical)

print(thisset)
```   

### Herhangi Bir Iterable Ekleme

update() metodundaki obje mutlaka bir set olmak zorunda değildir, herhangi bir iterable obje olabilir (tuple, list, dictionary vb.).

``` python 
thisset = {"apple", "banana", "cherry"}
mylist = ["kiwi", "orange"]

thisset.update(mylist)

print(thisset)
```  

### Python - Set Öğeleri Silme

Bir set’ten bir öğe silmek için remove() veya discard() metodunu kullanın.

``` python 
thisset = {"apple", "banana", "cherry"}

thisset.remove("banana")

print(thisset)
``` 

Not: Silinmek istenen öğe set içinde yoksa, remove() bir hata (error) oluşturur. 

``` python 
thisset = {"apple", "banana", "cherry"}

thisset.discard("banana")

print(thisset)
```  

Not: Silinmek istenen öğe set içinde yoksa, discard() hata (error) oluşturmaz.

Ayrıca bir öğeyi silmek için pop() metodunu da kullanabilirsiniz, ancak bu metod rastgele bir öğeyi siler, bu yüzden hangi öğenin silineceğinden emin olamazsınız.

pop() metodunun dönüş değeri, silinen öğedir. 

``` python 
thisset = {"apple", "banana", "cherry"}

x = thisset.pop()

print(x)

print(thisset)
``` 

Not: Set’ler sırasız (unordered) olduğu için, pop() metodunu kullandığınızda hangi öğenin silineceğini bilemezsiniz. 

clear() metodu, set’i boşaltır

``` python 
thisset = {"apple", "banana", "cherry"}

thisset.clear()

print(thisset)
```  

del anahtar kelimesi, set’i tamamen siler 


``` python 
thisset = {"apple", "banana", "cherry"}

del thisset

print(thisset)
``` 

### Python - Set Üzerinde Döngü 

Bir set’in öğeleri üzerinde for döngüsü (for loop) kullanarak döngü yapılabilir:

``` python 
thisset = {"apple", "banana", "cherry"}

for x in thisset:
  print(x)
```  

### Python - Set Birleştirme 

Python’da iki veya daha fazla set’i birleştirmenin birkaç yolu vardır.

- union() ve update() metodları, her iki set’in tüm öğelerini birleştirir.

- ntersection() metodu, sadece tekrar eden öğeleri (duplicates) tutar.

- difference() metodu, ilk set’te olup diğer set(ler)de olmayan öğeleri tutar.

- symmetric_difference() metodu, tekrar eden öğeler hariç tüm öğeleri tutar.

**union()**

union() metodu, her iki set’in tüm öğeleri ile yeni bir set döndürür. 

``` python 
set1 = {"a", "b", "c"}
set2 = {1, 2, 3}

set3 = set1.union(set2)
print(set3)
```  

union() metodunun yerine | operatörünü kullanabilirsiniz ve aynı sonucu alırsınız. 

``` python 
set1 = {"a", "b", "c"}
set2 = {1, 2, 3}

set3 = set1 | set2
print(set3)
```  

### Birden Fazla Set’i Birleştirme (Join Multiple Sets)

Tüm birleştirme metodları ve operatörleri, birden fazla set’i birleştirmek için kullanılabilir.

Bir metod kullanırken, parantez içine virgülle ayrılmış daha fazla set eklemeniz yeterlidir:

``` python 
set1 = {"a", "b", "c"}
set2 = {1, 2, 3}
set3 = {"John", "Elena"}
set4 = {"apple", "bananas", "cherry"}

myset = set1.union(set2, set3, set4)
print(myset)
```  
| operatörünü kullanırken, set’leri daha fazla | operatörü ile ayırın:


``` python 
set1 = {"a", "b", "c"}
set2 = {1, 2, 3}
set3 = {"John", "Elena"}
set4 = {"apple", "bananas", "cherry"}

myset = set1 | set2 | set3 |set4
print(myset)
``` 

#### Bir Set ve Tuple’i Birleştirme (Join a Set and a Tuple)

union() metodu, bir set’i list veya tuple gibi diğer veri tipleriyle birleştirmenizi sağlar.

Sonuç her zaman bir set olur.

``` python 
x = {"a", "b", "c"}
y = (1, 2, 3)

z = x.union(y)
print(z)
```  

Not: | operatörü, yalnızca set’leri set ile birleştirmenize izin verir; union() metodunda olduğu gibi diğer veri tipleriyle birleştiremezsiniz.

#### Update

update() metodu, bir set’in tüm öğelerini başka bir set’in içine ekler.

update(), orijinal set’i değiştirir ve yeni bir set döndürmez.

``` python 
set1 = {"a", "b" , "c"}
set2 = {1, 2, 3}

set1.update(set2)
print(set1)
``` 

Not: Hem union() hem de update() metodları, tekrar eden öğeleri (duplicates) hariç tutar. 

#### Intersection

Sadece tekrar eden öğeleri (duplicates) tutar.

intersection() metodu, her iki set’te bulunan öğeleri içeren yeni bir set döndürür.

``` python 
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}

set3 = set1.intersection(set2)
print(set3)
``` 

intersection() metodunun yerine & operatörünü kullanabilirsiniz ve aynı sonucu alırsınız. 

``` python 
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}

set3 = set1 & set2
print(set3)
```  

Not: & operatörü, yalnızca set’leri set ile birleştirmenize izin verir; intersection() metodunda olduğu gibi diğer veri tipleriyle birleştiremezsiniz. 

intersection_update() metodu da sadece tekrar eden öğeleri (duplicates) tutar, fakat yeni bir set döndürmek yerine orijinal set’i değiştirir. 

``` python 
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}

set1.intersection_update(set2)

print(set1)
```  

True ve 1 aynı değer olarak kabul edilir. Aynı şekilde, False ve 0 da aynı şekilde değerlendirilir. 

``` python 
# True, False, 1 ve 0 değerlerini içeren set’leri birleştirin ve hangi değerlerin tekrar (duplicate) olarak kabul edildiğini görün: 
set1 = {"apple", 1,  "banana", 0, "cherry"}
set2 = {False, "google", 1, "apple", 2, True}

set3 = set1.intersection(set2)

print(set3)
```  

#### Difference
difference() metodu, ilk set’te olup diğer set’te olmayan öğeleri içeren yeni bir set döndürür.

``` python 
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}

set3 = set1.difference(set2)

print(set3)
```  

difference() metodunun yerine - operatörünü kullanabilirsiniz ve aynı sonucu elde edersiniz.


``` python 
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}

set3 = set1 - set2
print(set3)
```  

Not: - operatörü, yalnızca set’leri set ile birleştirmenize izin verir; difference() metodunda olduğu gibi diğer veri tipleriyle birleştiremezsiniz. 


difference_update() metodu da ilk set’te olup diğer set’te olmayan öğeleri tutar, fakat yeni bir set döndürmek yerine orijinal set’i değiştirir. 

``` python 
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}

set1.difference_update(set2)

print(set1)
```  

#### Simetrik Fark (Symmetric Differences)

symmetric_difference() metodu, her iki set’te de bulunmayan öğeleri tutar.

``` python 
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}

set3 = set1.symmetric_difference(set2)

print(set3)
``` 

symmetric_difference() metodunun yerine ^ operatörünü kullanabilirsiniz ve aynı sonucu alırsınız.

``` python 
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}

set3 = set1 ^ set2
print(set3)
```  

Not: ^ operatörü, yalnızca set’leri set ile birleştirmenize izin verir; symmetric_difference() metodunda olduğu gibi diğer veri tipleriyle kullanılamaz. 

symmetric_difference_update() metodu da tekrar edenler hariç tüm öğeleri tutar, fakat yeni bir set döndürmek yerine orijinal set’i değiştirir. 


``` python 
set1 = {"apple", "banana", "cherry"}
set2 = {"google", "microsoft", "apple"}

set1.symmetric_difference_update(set2)

print(set1)
```  
### Frozenset 

frozenset, bir set’in değiştirilemez (immutable) versiyonudur.

Setler gibi, benzersiz (unique), sırasız (unordered) ve değiştirilemez (unchangeable) öğeler içerir.

Setlerin aksine, frozenset’e öğe eklenemez veya çıkarılamaz. 

#### frozenset Oluşturma

Herhangi bir iterable’dan frozenset oluşturmak için frozenset() yapıcısını (constructor) kullanın.

``` python 
x = frozenset({"apple", "banana", "cherry"})
print(x)
print(type(x))
``` 

### Frozenset Metodları 

| Metot (Method) | Kısayol (Shortcut) | Açıklama (Description) |
| :---: | :---: | :--- |
| **copy()** | | Sığ bir kopya (shallow copy) döndürür. |
| **difference()** | **-** | Farkı içeren yeni bir frozenset döndürür. |
| **intersection()** | **&** | Kesişimi içeren yeni bir frozenset döndürür. |
| **isdisjoint()** | | İki frozenset'in kesişimi olup olmadığını döndürür. |
| **issubset()** | **<= / <** | Bu frozenset'in bir (gerçek) alt küme olup olmadığını Doğru (True) döndürür. |
| **issuperset()** | **>= / >** | Bu frozenset'in bir (gerçek) üst küme olup olmadığını Doğru (True) döndürür. |
| **symmetric_difference()** | **^** | Simetrik farkları içeren yeni bir frozenset döndürür. |
| **union()** | **\|** | Birleşimi içeren yeni bir frozenset döndürür. |

### Set Metodları 

| Metot (Method) | Kısayol (Shortcut) | Açıklama (Description) |
| :---: | :---: | :--- |
| **add()** | | Kümeye bir öğe ekler. |
| **clear()** | | Kümedeki tüm öğeleri kaldırır. |
| **copy()** | | Kümenin bir kopyasını döndürür. |
| **difference()** | **-** | İki veya daha fazla küme arasındaki farkı içeren bir küme döndürür. |
| **difference_update()** | **-=** | Bu kümede olup, başka bir belirtilen kümede de bulunan öğeleri kaldırır. |
| **discard()** | | Belirtilen öğeyi kaldırır. |
| **intersection()** | **&** | Diğer iki kümenin kesişimi olan bir küme döndürür. |
| **intersection_update()** | **&=** | Diğer belirtilen kümelerde bulunmayan öğeleri kaldırır. |
| **isdisjoint()** | | İki kümenin kesişimi olup olmadığını döndürür. |
| **issubset()** | **<=** | Bu kümenin tüm öğeleri başka bir kümede varsa Doğru (True) döndürür. |
| **issubset()** | **<** | Bu kümenin tüm öğeleri başka, daha büyük bir kümede varsa Doğru (True) döndürür. |
| **issuperset()** | **>=** | Tüm diğer kümelerin öğeleri bu kümede varsa Doğru (True) döndürür. |
| **issuperset()** | **>** | Diğer, daha küçük bir kümenin tüm öğeleri bu kümede varsa Doğru (True) döndürür. |
| **pop()** | | Kümeden rastgele bir öğe kaldırır. |
| **remove()** | | Belirtilen öğeyi kaldırır. |
| **symmetric_difference()** | **^** | İki kümenin simetrik farklarını içeren bir küme döndürür. |
| **symmetric_difference_update()** | **^=** | Bu küme ve diğer kümenin simetrik farklarını ekler. |
| **union()** | **\|** | Kümelerin birleşimini içeren bir küme döndürür. |
| **update()** | **\|=** | Bu küme ve diğerlerinin birleşimi ile kümeyi günceller. |

## Python Dictionaries 

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
```  
Dictionaries, veri değerlerini anahtar:değer (key:value) çiftleri halinde saklamak için kullanılır.

Bir dictionary, sıralı (ordered*), değiştirilebilir (changeable) ve tekrar eden öğelere izin vermez (do not allow duplicates) bir koleksiyondur. 

Dictionaries, süslü parantez {} ile yazılır ve keys (anahtarlar) ile values (değerler) içerir: 


``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
print(thisdict)
```  

#### Dictionary Items

Dictionary öğeleri sıralıdır (ordered), değiştirilebilir (changeable) ve tekrar eden öğelere izin vermez (do not allow duplicates).

Dictionary öğeleri key:value çiftleri olarak sunulur ve key adı kullanılarak erişilebilir.

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
print(thisdict["brand"])
```   

#### Ordered or Unordered?

Python 3.7 ve sonrasında, dictionaries sıralıdır (ordered). Python 3.6 ve öncesinde dictionaries sırasızdır (unordered).

Ordered demek, öğelerin belirli bir sıraya sahip olduğu ve bu sıranın değişmeyeceği anlamına gelir.

Unordered demek ise, öğelerin belirli bir sıraya sahip olmadığı ve bir öğeye indeks kullanarak erişilemeyeceği anlamına gelir. 

#### Changeable

Dictionaries değiştirilebilir (changeable), yani bir dictionary oluşturulduktan sonra öğeleri değiştirebilir, ekleyebilir veya silebiliriz.

### Duplicates Not Allowed

Dictionaries aynı key değerine sahip iki öğe içeremez.

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964,
  "year": 2020
}
print(thisdict)
``` 

#### Dictionary Length

Bir dictionary’nin kaç öğe içerdiğini öğrenmek için len() fonksiyonunu kullanın:

``` python 
print(len(thisdict))
``` 

#### Dictionary Items - Data Types

Dictionary içindeki values herhangi bir veri tipi olabilir:

``` python 
thisdict = {
  "brand": "Ford",
  "electric": False,
  "year": 1964,
  "colors": ["red", "white", "blue"]
}
``` 

**type()**

Python açısından, dictionaries 'dict' veri tipine sahip nesneler olarak tanımlanır:

``` python 
<class 'dict'>
```  

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
print(type(thisdict))
``` 

#### The dict() Constructor

Bir dictionary oluşturmak için dict() constructor’ını da kullanabilirsiniz.

``` python 
thisdict = dict(name = "John", age = 36, country = "Norway")
print(thisdict)
```  
### Python - Dictionary Öğelerine Erişme 

Bir dictionary öğesine, köşeli parantez içinde anahtar adıyla erişilebilir: 

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
x = thisdict["model"]
```  

Ayrıca aynı sonucu verecek get() adında bir yöntem de vardır: 

``` python 
x = thisdict.get("model")
```   

### Get Keys

keys() yöntemi, sözlükteki tüm anahtarların bir listesini döndürür. 

``` python 
x = thisdict.keys()
```    

Anahtarların listesi, sözlüğün bir görünümüdür (view). Bu, sözlükte yapılan herhangi bir değişikliğin, anahtarlar listesinde de yansıyacağı anlamına gelir. 

``` python 
car = {
"brand": "Ford",
"model": "Mustang",
"year": 1964
}

x = car.keys()

print(x) #değişimden önce

car["color"] = "white"

print(x) #değişimden sonra
```

#### Value'leri alma

values() metodu, sözlükteki tüm değerlerin bir listesini döndürecektir. 

``` python 
x = thisdict.values()
``` 

Değerler listesinin sözlüğün bir görüntüsü (view) olduğunu unutmayın; yani sözlükte yapılan herhangi bir değişiklik, değerler listesinde de yansıyacaktır. 

Orijinal sözlükte bir değişiklik yapın ve değerler listesinin de güncellendiğini görün.

``` python 
car = {
"brand": "Ford",
"model": "Mustang",
"year": 1964
}

x = car.values()

print(x) #değişimden önce

car["year"] = 2020

print(x) #değişimden sonra
``` 
Orijinal sözlüğe yeni bir öğe ekleyin ve değerler listesinin de güncellendiğini görün. 

``` python 
car = {
"brand": "Ford",
"model": "Mustang",
"year": 1964
}

x = car.values()

print(x) #değişimden önce

car["color"] = "red"

print(x) #değişimden sonra
```  
### Get Items
items() metodu, bir dictionary içindeki her öğeyi, bir listede tuple’lar olarak döndürür.


``` python 
x = thisdict.items()
``` 

Döndürülen liste, dictionary’nin öğelerinin bir görünümüdür; yani dictionary üzerinde yapılan herhangi bir değişiklik, items listesine de yansır. 


``` python 
car = {
"brand": "Ford",
"model": "Mustang",
"year": 1964
}

x = car.items()

print(x) #değişimden önce

car["year"] = 2020

print(x) #değişimden sonra
```

``` python 
car = {
"brand": "Ford",
"model": "Mustang",
"year": 1964
}

x = car.items()

print(x) #değişimden önce

car["color"] = "red"

print(x) #değişimden sonra
```

### Key'in Var Olup Olmadığını Kontrol Et

Belirli bir key’in bir dictionary içinde bulunup bulunmadığını kontrol etmek için in keyword’ünü kullan:

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
if "model" in thisdict:
  print("Evet, 'model' bu thisdict dictionary’sindeki key’lerden biridir.")
``` 

### Python - Dictionary Öğelerini Değiştirme

**Değerleri Değiştir**

Belirli bir öğenin değerini, onun key adını kullanarak değiştirebilirsin:

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
thisdict["year"] = 2018
``` 

### Upatade Dictionary

update() metodu, verilen argümandaki öğelerle dictionary’yi günceller.

Argüman bir dictionary olmalı ya da key:value çiftleri içeren bir iterable nesne olmalıdır. 

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
thisdict.update({"year": 2020})
```

### Python - Dictionary’ye Öğeler Ekleme 

Dictionary’ye bir öğe eklemek, yeni bir key oluşturarak ve ona bir değer atayarak yapılır:

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
thisdict["color"] = "red"
print(thisdict)
``` 

#### Upatade Dictionary
update() metodu, verilen argümandaki öğelerle dictionary’yi günceller. Eğer öğe mevcut değilse, yeni öğe eklenir.

Argüman bir dictionary olmalı ya da key:value çiftleri içeren bir iterable nesne olmalıdır.

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
thisdict.update({"color": "red"})
```  

### Python - Dictionary’den Öğeleri Kaldırma

Dictionary’den öğe kaldırmak için birkaç farklı yöntem var: 

pop() metodu, belirtilen key’e sahip öğeyi kaldırır:

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
thisdict.pop("model")
print(thisdict)
``` 

popitem() metodu, en son eklenen öğeyi kaldırır:

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
thisdict.popitem()
print(thisdict)
``` 
del keyword’ü, belirtilen key’e sahip öğeyi kaldırır: 

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
del thisdict["model"]
print(thisdict)
```  

del keyword’ü, dictionary’yi tamamen silmek için de kullanılabilir:

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
del thisdict
print(thisdict) #Bu bir hata oluşturur çünkü artık thisdict dictionary’si mevcut değil:
```
clear() metodu, dictionary’yi boşaltır:

``` python 
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
thisdict.clear()
print(thisdict)
``` 

### Python - Dictionary’lerde Döngü Kullanma

Bir dictionary üzerinde for döngüsü kullanarak döngü kurabilirsin.

Dictionary üzerinde döngü kurulduğunda döngüde dönen değerler dictionary’nin key’leri olur, fakat değerleri almak için de bazı yöntemler vardır. 

``` python
# bütün key'leri tek tek yazdırır
for x in thisdict:
  print(x)
``` 

``` python
# bütün değerleri tek tek yazdırır
for x in thisdict:
  print(thisdict[x])
``` 

Ayrıca values() metodunu kullanarak bir dictionary’nin değerlerini döndürebilirsin: 

``` python
for x in thisdict.values():
  print(x)
``` 

keys() metodunu kullanarak bir dictionary’nin key’lerini döndürebilirsin: 

``` python
for x in thisdict.keys():
  print(x)
```

items() metodunu kullanarak hem key hem de value üzerinde döngü kurabilirsin:

``` python
for x, y in thisdict.items():
  print(x, y)
```

### Python - Dictionary Kopyalama

Bir dictionary’yi basitçe dict2 = dict1 yazarak kopyalayamazsın, çünkü bu durumda dict2 sadece dict1’e bir referans olur ve dict1’de yapılan değişiklikler otomatik olarak dict2’ye de yansır.

Bir kopya oluşturmanın yolları vardır; bunlardan biri, yerleşik copy() metodunu kullanmaktır.

``` python
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
mydict = thisdict.copy()
print(mydict)
``` 

Bir diğer kopya oluşturma yöntemi, yerleşik dict() fonksiyonunu kullanmaktır. 

``` python
thisdict = {
  "brand": "Ford",
  "model": "Mustang",
  "year": 1964
}
mydict = dict(thisdict)
print(mydict)
``` 

### Python - İç İçe (Nested) Dictionary’ler 

Bir dictionary başka dictionary’ler içerebilir; buna iç içe (nested) dictionary denir.

``` python
myfamily = {
  "child1" : {
    "name" : "Emil",
    "year" : 2004
  },
  "child2" : {
    "name" : "Tobias",
    "year" : 2007
  },
  "child3" : {
    "name" : "Linus",
    "year" : 2011
  }
}
``` 
Ya da, üç dictionary’yi yeni bir dictionary içine eklemek için:


``` python
child1 = {
  "name" : "Emil",
  "year" : 2004
}
child2 = {
  "name" : "Tobias",
  "year" : 2007
}
child3 = {
  "name" : "Linus",
  "year" : 2011
}

myfamily = {
  "child1" : child1,
  "child2" : child2,
  "child3" : child3
}
``` 

#### İç İçe Dictionary’de Öğelere Erişme

İç içe bir dictionary’den öğelere erişmek için, dış dictionary’den başlayarak dictionary isimlerini kullanırsın:

``` python
print(myfamily["child2"]["name"])
``` 

#### İç İçe Dictionary’lerde Döngü

items() metodunu kullanarak bir dictionary üzerinde döngü kurulabilir, örneğin:

``` python
for x, obj in myfamily.items():
  print(x)

  for y in obj:
    print(y + ':', obj[y])
``` 

### Dictionary Metodları 


| Metot (Method) | Açıklama (Description) |
| :---: | :--- |
| **clear()** | Sözlükteki tüm öğeleri kaldırır. |
| **copy()** | Sözlüğün bir kopyasını döndürür. |
| **fromkeys()** | Belirtilen anahtarlar ve değer ile bir sözlük döndürür. |
| **get()** | Belirtilen anahtarın değerini döndürür. |
| **items()** | Her anahtar-değer çifti için bir demet (tuple) içeren bir liste döndürür. |
| **keys()** | Sözlüğün anahtarlarını içeren bir liste döndürür. |
| **pop()** | Belirtilen anahtara sahip öğeyi kaldırır. |
| **popitem()** | Son eklenen anahtar-değer çiftini kaldırır. |
| **setdefault()** | Belirtilen anahtarın değerini döndürür. Eğer anahtar mevcut değilse, anahtarı belirtilen değerle ekler. |
| **update()** | Sözlüğü belirtilen anahtar-değer çiftleriyle günceller. |
| **values()** | Sözlükteki tüm değerlerin bir listesini döndürür. |

## Python - If (Koşul) İfadesi

#### Python Koşullar ve If İfadeleri

Python, matematikteki yaygın mantıksal koşulları destekler:

- Eşit: a == b

- Eşit Değil: a != b

- Küçüktür: a < b

- Küçük veya eşittir: a <= b

- Büyüktür: a > b

- Büyük veya eşittir: a >= b

Bu koşullar birçok şekilde kullanılabilir; en yaygın kullanım alanları if ifadeleri ve döngülerdir.

Bir if ifadesi, if keyword’ü kullanılarak yazılır. 

``` python
a = 33
b = 200
if b > a:
  print("b a'dan büyük")
```  

Bu örnekte iki değişken, a ve b, kullanılıyor ve if ifadesi içinde b’nin a’dan büyük olup olmadığını test ediyoruz.

a 33, b 200 olduğundan, 200’nin 33’ten büyük olduğu bellidir ve ekrana "b a'dan büyük" yazdırılır.


#### If İfadeleri Nasıl Çalışır

If ifadesi, bir koşulu (True veya False değerine sahip bir ifade) değerlendirir. Koşul True ise, if bloğu içindeki kod çalıştırılır. Koşul False ise, if bloğu atlanır ve çalıştırılmaz.

``` python
number = 15
if number > 0:
  print("The number is positive")
``` 

### Girintileme (Indentation)

Python, kod bloğunun kapsamını (scope) belirlemek için girintilemeye (satır başındaki boşluklara) dayanır. Diğer programlama dilleri genellikle bunu küme parantezleri {} ile yapar.

Python’da if bloğu girintilenmezse, bir IndentationError oluşur çünkü Python hangi kodun if bloğuna ait olduğunu anlayamaz.

``` python
a = 33
b = 200
if b > a:
print("b is greater than a") 
# Çıktı 

#   File "demo_if_error.py", line 4
#     print("b is greater than a")
#         ^
# IndentationError: expected an indented block
```  

Not: Girintileme için boşluk veya tab kullanılabilir, fakat aynı kod bloğu içindeki tüm satırlarda aynı miktarda girintileme kullanılmalıdır. 

#### If Bloğunda Birden Fazla İfade

Bir if bloğu içinde birden fazla ifade bulunabilir. Tüm ifadeler aynı seviyede girintilenmiş olmalıdır.

``` python
age = 20
if age >= 18:
  print("You are an adult")
  print("You can vote")
  print("You have full legal rights")
```  

#### Koşullarda Değişken Kullanımı

Boolean değişkenler, karşılaştırma operatörleri kullanmadan doğrudan if ifadelerinde kullanılabilir.

``` python
is_logged_in = True
if is_logged_in:
  print("Welcome back!")
```  

Python, birçok değeri if ifadesinde True veya False olarak değerlendirebilir.

False olarak değerlendirilenler:

+ Sıfır (0)
+ Boş string ("")
+ None
+ Boş koleksiyonlar (ör. [], {}, ())

True olarak değerlendirilenler:

+ Pozitif sayılar (ör. 5)
+ Negatif sayılar (ör. -3)
+ Boş olmayan stringler (ör. "False" bile True olarak değerlendirilir çünkü boş değildir)

### Python - Elif İfadesi 

elif keyword’ü, Python’da “önceki koşullar doğru değilse, bu koşulu dene” anlamına gelir.

elif, birden fazla ifadeyi True olup olmadığını kontrol etmek ve koşullardan biri True olduğunda ilgili kod bloğunu çalıştırmak için kullanılır. 

``` python
a = 33
b = 33
if b > a:
  print("b is greater than a")
elif a == b:
  print("a and b are equal")
```

Bu örnekte a, b’ye eşittir; bu nedenle ilk koşul doğru değildir, fakat elif koşulu doğrudur. Sonuç olarak ekrana "a and b are equal" yazdırılır. 

### Birden Fazla Elif İfadesi

İhtiyacın olduğu kadar elif ifadesi kullanabilirsin. Python, koşulları sırayla kontrol eder ve ilk doğru olan koşulun kod bloğunu çalıştırır.

``` python
score = 75

if score >= 90:
  print("Grade: A")
elif score >= 80:
  print("Grade: B")
elif score >= 70:
  print("Grade: C")
elif score >= 60:
  print("Grade: D")
``` 

Bu örnekte program, koşulları sırayla kontrol eder. score 75 olduğundan, ilk doğru koşul olan "Grade: C" yazdırılır. 

### Elif Nasıl Çalışır

elif kullanıldığında Python, koşulları yukarıdan aşağıya değerlendirir. Doğru bir koşul bulduğu anda, ilgili kod bloğunu çalıştırır ve kalan tüm koşulları atlar.

Not: Sadece ilk doğru koşul çalıştırılır. Birden fazla koşul doğru olsa bile, Python ilk eşleşen bloğu çalıştırdıktan sonra durur.

``` python
score = 75
age = 25

if age < 13:
  print("You are a child")
elif age < 20:
  print("You are a teenager")
elif age < 65:
  print("You are an adult")
elif age >= 65:
  print("You are a senior")
``` 

### Elif Ne Zaman Kullanılır

Birden fazla birbirini dışlayan koşulu kontrol etmen gerektiğinde elif kullan. Bu, birden fazla ayrı if ifadesi kullanmaktan daha verimlidir, çünkü Python doğru koşulu bulduğunda diğerlerini kontrol etmeyi bırakır.

``` python
day = 3

if day == 1:
  print("Monday")
elif day == 2:
  print("Tuesday")
elif day == 3:
  print("Wednesday")
elif day == 4:
  print("Thursday")
elif day == 5:
  print("Friday")
elif day == 6:
  print("Saturday")
elif day == 7:
  print("Sunday")
```  

### Python - Else İfadesi

Else keyword’ü, önceki koşullar tarafından yakalanmayan durumları kapsar.

Else bloğu, if koşulu ve varsa elif koşulları False olduğunda çalıştırılır

``` python
a = 200
b = 33
if b > a:
  print("b is greater than a")
elif a == b:
  print("a and b are equal")
else:
  print("a is greater than b")
```   

Bu örnekte a, b’den büyüktür; bu yüzden ilk koşul doğru değildir, elif koşulu da doğru değildir. Bu durumda else bloğu çalışır ve ekrana "a is greater than b" yazdırılır. 

Elif kullanmadan da bir else bloğu eklenebilir:

``` python
a = 200
b = 33
if b > a:
  print("b is greater than a")
else:
  print("b is not greater than a")
```  


Bu, basit bir iki yönlü seçim oluşturur: koşul True ise bir blok çalıştırılır; aksi halde else bloğu çalıştırılır. 

### Else Nasıl Çalışır

Else ifadesi, önceki koşulların hiçbiri doğru olmadığında varsayılan bir işlem sağlar. Bunu, if ve elif ifadelerinin kapsamadığı tüm durumlar için bir “yakala-hepsini” bloğu olarak düşünebilirsin.

Not: Else ifadesi her zaman en sonunda olmalıdır. Else’den sonra elif kullanamazsın. 

``` python
number = 7

if number % 2 == 0:
  print("The number is even")
else:
  print("The number is odd")
```  

### If-Elif-Else Zinciri Tamamlama:

If, elif ve else ifadelerini birleştirerek kapsamlı bir karar verme yapısı oluşturabilirsin.

``` python
temperature = 22

if temperature > 30:
  print("It's hot outside!")
elif temperature > 20:
  print("It's warm outside")
elif temperature > 10:
  print("It's cool outside")
else:
  print("It's cold outside!")
``` 

#### Else’i Yedek Olarak Kullanma

Else ifadesi, önceki koşulların hiçbiri doğru olmadığında çalışacak bir yedek görevi görür. Bu, hata yönetimi, doğrulama ve varsayılan değer sağlama gibi durumlarda faydalıdır.

``` python
username = "Emil"

if len(username) > 0:
  print(f"Welcome, {username}!")
else:
  print("Error: Username cannot be empty")
``` 

### Python - Tek Satırlık (Shorthand) If 

Eğer çalıştırılacak tek bir ifade varsa, bunu if ifadesiyle aynı satıra yazabilirsin.

``` python
a = 5
b = 2
if a > b: print("a is greater than b")
```  
Not: Koşuldan sonra yine de iki nokta üst üste (:) kullanman gerekir. 

### Tek Satırlık If ... Else

Eğer if için bir ifade ve else için bir ifade varsa, bunları koşullu ifade (conditional expression) kullanarak aynı satıra yazabilirsin:

``` python
a = 2
b = 330
print("A") if a > b else print("B")
``` 

Buna koşullu ifade denir (bazı kaynaklarda “üçlü operatör” veya ternary operator olarak adlandırılır). 

### If ... Else ile Değer Atama

Tek satırlık if/else ifadesi kullanarak bir değer seçebilir ve bunu bir değişkene atayabilirsin:

``` python
a = 10
b = 20
bigger = a if a > b else b
print("Bigger is", bigger)
``` 

syntax şu şekilde olur: 

```
variable = value_if_true if condition else value_if_false
``` 

#### Tek Satırda Birden Fazla Koşul
Koşullu ifadeleri zincirleyebilirsin, fakat okunabilirliği korumak için kısa tutulmsdı gerek.

``` python
a = 330
b = 330
print("A") if a > b else print("=") if a == b else print("B")
```  

**Pratik Örnekler**

Ternary operator (üçlü operatör) özellikle basit değer atamaları ve return ifadeleri için kullanışlıdır. 

İki Sayının En Büyüğünü Bulma:

``` python
x = 15
y = 20
max_value = x if x > y else y
print("Maximum value:", max_value)
```  
Varsayılan Değer Atama:

``` python
username = ""
display_name = username if username else "Guest"
print("Welcome,", display_name)
```  

### Python - Mantıksal (Logical) Operatörler

Mantıksal operatörler, koşullu ifadeleri birleştirmek için kullanılır. Python’da üç mantıksal operatör vardır:

- and – Her iki ifade de doğruysa True döner

- or – İfadelerden biri doğruysa True döner

- not – Sonucu tersine çevirir; doğruysa False, yanlışsa True döner

### And Operatörü

And keyword’ü bir mantıksal operatördür ve koşullu ifadeleri birleştirmek için kullanılır. Tüm ifadenin True olabilmesi için her iki koşulun da doğru olması gerekir.

``` python
a = 200
b = 33
c = 500
if a > b and c > a:
  print("Both conditions are True")
``` 

### Or Operatörü

Or keyword’ü bir mantıksal operatördür ve koşullu ifadeleri birleştirmek için kullanılır. Tüm ifadenin True olabilmesi için en az bir koşulun doğru olması yeterlidir.

``` python
a = 200
b = 33
c = 500
if a > b or a > c:
  print("At least one of the conditions is True")
```  
### Not Operatörü

Not keyword’ü bir mantıksal operatördür ve koşullu ifadenin sonucunu tersine çevirmek için kullanılır.

``` python
a = 33
b = 200
if not a > b:
  print("a is NOT greater than b")
``` 
#### Birden Fazla Operatörü Birleştirme

Tek bir ifadede birden fazla mantıksal operatörü birleştirebilirsiniz. Python önce not, sonra and, sonra or ifadeleri değerlendirir.

``` python
age = 25
is_student = False
has_discount_code = True

if (age < 18 or age > 65) and not is_student or has_discount_code:
  print("Discount applies!")
``` 

### Doğruluk Tabloları

Mantıksal operatörlerin farklı değerlerle nasıl çalıştığını anlamak için:

#### and operatörü doğruluk tablosu 

| Durum 1 (Condition 1) | Durum 2 (Condition 2) | Sonuç (Result) |
| :---: | :---: | :---: |
| True (Doğru) | True (Doğru) | True (Doğru) |
| True (Doğru) | False (Yanlış) | False (Yanlış) |
| False (Yanlış) | True (Doğru) | False (Yanlış) |
| False (Yanlış) | False (Yanlış) | False (Yanlış) | 

#### or operatörü doğruluk tablosu

| Durum 1 (Condition 1) | Durum 2 (Condition 2) | Sonuç (Result) |
| :---: | :---: | :---: |
| True (Doğru) | True (Doğru) | True (Doğru) |
| True (Doğru) | False (Yanlış) | True (Doğru) |
| False (Yanlış) | True (Doğru) | True (Doğru) |
| False (Yanlış) | False (Yanlış) | False (Yanlış) | 

**Anlamı Açık Hale Getirmek İçin Parantez Kullanma**

Birden fazla mantıksal operatör birleştirirken, niyetini netleştirmek ve değerlendirme sırasını kontrol etmek için parantez kullan. 

``` python
temperature = 25
is_raining = False
is_weekend = True

if (temperature > 20 and not is_raining) or is_weekend:
  print("Great day for outdoor activities!")
```  
### Python - İç İçe (Nested) If

Bir if ifadesinin içinde başka bir if ifadesi kullanabilirsin; buna iç içe (nested) if denir. 

``` python
x = 41

if x > 10:
  print("Above ten,")
  if x > 20:
    print("and also above 20!")
  else:
    print("but not above 20.")
``` 

Bu örnekte, içteki if yalnızca dış koşul (x > 10) doğru olduğunda çalışır. 

#### İç İçe If Nasıl Çalışır

Her iç içe seviye, daha derin bir karar verme katmanı oluşturur. Kod, en dıştaki koşuldan içe doğru değerlendirilir.

``` python
age = 25
has_license = True

if age >= 18:
  if has_license:
    print("You can drive")
  else:
    print("You need a license")
else:
  print("You are too young to drive")
``` 

### İç İçe If vs Mantıksal Operatörler

Bazen iç içe if ifadeleri, and gibi mantıksal operatörler kullanılarak basitleştirilebilir. Hangi yöntemi kullanacağın, mantığına bağlıdır.

``` python
temperature = 25
is_sunny = True

if temperature > 20:
  if is_sunny:
    print("Perfect beach weather!")
```  

Ayrıca and kullanılarak şöyle yazılabilir:

``` python
temperature = 25
is_sunny = True

if temperature > 20 and is_sunny:
  print("Perfect beach weather!")
```  

Her iki yaklaşım da aynı sonucu verir. İç mantık karmaşık olduğunda veya dış koşula bağlı olduğunda iç içe geçmiş if ifadeleri kullanın. Her iki koşul da basit ve eşit derecede önemli olduğunda kullanın. 

Giriş doğrulama (login) için iç içe kontroller: 

``` python
username = "Emil"
password = "python123"
is_active = True

if username:
  if password:
    if is_active:
      print("Login successful")
    else:
      print("Account is not active")
  else:
    print("Password required")
else:
  print("Username required")
```  
Not hesaplama için iç içe mantık: 

``` python
score = 92
extra_credit = 5

if score >= 90:
  if extra_credit > 0:
    print("A+ grade")
  else:
    print("A grade")
elif score >= 80:
  print("B grade")
else:
  print("C grade or below")
``` 

### Python - Pass İfadesi

If ifadeleri boş bırakılamaz; ancak herhangi bir nedenle içeriği olmayan bir if ifadesine sahip olursan, pass ifadesini kullanarak hata oluşmasını önleyebilirsin. 

``` python
a = 33
b = 200

if b > a:
  pass
``` 

Pass ifadesi bir null işlemdir — çalıştırıldığında hiçbir şey yapmaz. Genellikle yer tutucu olarak kullanılır. 

### Pass Neden Kullanılır?

Pass ifadesi şu durumlarda faydalıdır:

- Kod yapısını oluştururken, mantığı henüz yazmadıysan

- Sözdizimsel olarak bir ifade gerekli ama işlem yapılmayacaksa

- Geliştirme sırasında gelecekteki kod için yer tutucu olarak

- Daha sonra uygulamayı planladığın boş fonksiyonlar veya sınıflar için

#### Geliştirme Sırasında Pass

Geliştirme aşamasında, program yapısını detayları yazmadan tasarlamak isteyebilirsin. Pass ifadesi, bunu sözdizimi hatası olmadan yapmanı sağlar.

``` python
age = 16

if age < 18:
  pass # TODO: Add underage logic later
else:
  print("Access granted")
```  
### Pass vs Yorum Satırı

Bir yorum satırı Python tarafından görmezden gelinir, ancak pass gerçek bir ifadedir ve çalıştırılır (her ne kadar hiçbir şey yapmasa da). Python’un bir ifade beklediği yerde, yalnızca yorum satırı yeterli olmaz; pass kullanman gerekir.

Bu bir hata oluşturur (boş kod bloğu): 

``` python
score = 85

if score > 90:
  # Bu mükemmel
# Bu, bir IndentationError (Girinti Hatası) oluşturur.
```

Pass kullanıldığında bu doğru şekilde çalışır:

``` python
score = 85

if score > 90:
  pass # This is excellent
print("Score processed")
``` 

### Birden Fazla Koşulda Pass

If-elif-else yapısının herhangi bir dalında pass kullanılabilir. 

``` python
value = 50

if value < 0:
  print("Negative value")
elif value == 0:
  pass # Sıfır durumu – herhangi bir işlem gerekmez.
else:
  print("Positive value")
``` 

#### Pass’in Diğer Kullanım Alanları

Burada if ifadeleri ile kullanımına odaklansak da, pass aynı zamanda döngüler, fonksiyonlar ve sınıflar ile de sıkça kullanılır.

``` python
def calculate_discount(price):
  pass 
# Fonksiyon mevcut ama henüz hiçbir şey yapmıyor
``` 

## Python - While Döngüleri 

Python’da iki temel döngü komutu vardır:

- while döngüleri
- for döngüleri

While döngüsü ile, bir koşul doğru olduğu sürece bir grup ifadeyi tekrar tekrar çalıştırabiliriz. 

``` python
i = 1
while i < 6:
  print(i)
  i += 1
```  

While döngüsü, ilgili değişkenlerin hazır olmasını gerektirir. Bu örnekte, bir indeks değişkeni olan i’yi tanımlamamız gerekiyor; başlangıç değeri olarak 1 atıyoruz. 

### Break İfadesi

Break ifadesi ile, while koşulu doğru olsa bile döngüyü durdurabiliriz.

``` python
i = 1
while i < 6:
  print(i)
  if i == 3:
    break
  i += 1
```  

### Continue İfadesi

Continue ifadesi ile, mevcut iterasyonu durdurup, döngünün bir sonraki adımıyla devam edebiliriz

``` python
i = 0
while i < 6:
  i += 1
  if i == 3:
    continue
  print(i)
```  

### Else İfadesi

Else ifadesi ile, döngü koşulu artık doğru olmadığında, bir kod bloğunu bir kez çalıştırabiliriz.

``` python
i = 1
while i < 6:
  print(i)
  i += 1
else:
  print("i is no longer less than 6")
``` 

## Python - For Döngüleri

For döngüsü, bir dizi üzerinde yineleme yapmak için kullanılır (bu dizi bir liste, demet (tuple), sözlük, küme veya string olabilir).

Diğer programlama dillerindeki for anahtar kelimesinden biraz farklıdır; daha çok nesne yönelimli dillerdeki bir iterator (yineleyici) yöntemi gibi çalışır.

For döngüsü ile, bir liste, demet, küme vb. içindeki her öğe için bir kod bloğunu bir kez çalıştırabiliriz. 

``` python
fruits = ["apple", "banana", "cherry"]
for x in fruits:
  print(x)
``` 

For döngüsü, önceden bir indeks değişkeni tanımlamanı gerektirmez. 

#### Bir String Üzerinde Döngü

Stringler de iterable (yinelemeli) nesnelerdir; bir karakter dizisi içerirler.

``` python
for x in "banana":
  print(x)
``` 

### Break İfadesi

Break ifadesi ile, döngü tüm öğeleri tamamlamadan durdurulabilir.

x değeri "banana" olduğunda döngüden çık: 

``` python
fruits = ["apple", "banana", "cherry"]
for x in fruits:
  print(x)
  if x == "banana":
    break
```  

x değeri "banana" olduğunda döngüden çık, bu sefer break ifadesi print’ten önce: 

``` python
fruits = ["apple", "banana", "cherry"]
for x in fruits:
  if x == "banana":
    break
  print(x)
```  

### Continue İfadesi

Continue ifadesi ile, döngünün mevcut iterasyonunu durdurup, bir sonraki adımına geçebiliriz.

``` python
fruits = ["apple", "banana", "cherry"]
for x in fruits:
  if x == "banana":
    continue
  print(x)
```  

### range() Fonksiyonu

Belirli sayıda kodu çalıştırmak için range() fonksiyonunu kullanabiliriz. 

Bu fonksiyon, varsayılan olarak 0’dan başlayan, 1’er artan ve belirtilen bir sayı ile biten bir sayı dizisi döndürür.

``` python
for x in range(6):
  print(x)
``` 

Not: range(6) ifadesi 0’dan 6’ya kadar olan değerleri değil, 0’dan 5’e kadar olan değerleri üretir.

range() fonksiyonu varsayılan olarak 0’dan başlar, ancak bir parametre ekleyerek başlangıç değerini belirlemek mümkündür. Örneğin, range(2, 6) ifadesi 2’den başlayıp 6’ya kadar (6 dahil değil) olan değerleri üretir. 

``` python
for x in range(2, 6):
  print(x)
```  

range() fonksiyonu varsayılan olarak diziyi 1’er 1’er artırır, ancak üçüncü bir parametre ekleyerek artış miktarını belirlemek mümkündür. Örneğin, range(2, 30, 3) ifadesi 2’den başlayıp 30’a kadar 3’er 3’er artan değerleri üretir. 

``` python
for x in range(2, 30, 3):
  print(x)
```  

### For Döngüsünde Else

Bir for döngüsünde else anahtar kelimesi, döngü normal şekilde tamamlandığında çalıştırılacak bir kod bloğunu belirtir.

``` python
for x in range(6):
  print(x)
else:
  print("Finally finished!")
```  

Not: Eğer döngü break ifadesi ile durdurulursa, else bloğu çalıştırılmaz 

``` python
for x in range(6):
  if x == 3: break
  print(x)
else:
  print("Finally finished!")
```  

### İç İçe Döngüler 

İç içe döngü, bir döngünün başka bir döngü içinde yer almasıdır.
“İç döngü” (inner loop), “dış döngü”nün (outer loop) her bir iterasyonu için bir kez çalıştırılır.

``` python
adj = ["red", "big", "tasty"]
fruits = ["apple", "banana", "cherry"]

for x in adj:
  for y in fruits:
    print(x, y)
``` 

### pass İfadesi

Bir for döngüsü boş bırakılamaz, ancak eğer herhangi bir nedenle içeriği olmayan bir döngüye sahipsen, hata almamak için pass ifadesini kullanabilirsin.

``` python
for x in [0, 1, 2]:
  pass
```  

## Python Fonksiyonlar

Bir fonksiyon, yalnızca çağrıldığında çalışan bir kod bloğudur.

Fonksiyonlar bir sonuç olarak veri döndürebilir.

Fonksiyon kullanmak, kod tekrarını önlemeye yardımcı olur. 

### Fonksiyon Oluşturma

Python’da bir fonksiyon, def anahtar kelimesi ile tanımlanır; ardından fonksiyon adı ve parantezler yazılır.

``` python
def greet():
  print("Hello from a function")
```  

Bu, çağrıldığında ekrana "Hello!" yazdıran my_function adlı bir fonksiyon oluşturur. 

Fonksiyon içindeki kod girintili olmalıdır. Python, kod bloklarını tanımlamak için girintiyi kullanır. 

### Fonksiyon Çağırma

Bir fonksiyonu çağırmak için, fonksiyon adını yazın ve ardından parantezleri () ekleyin.

``` python
def my_function():
  print("Hello from a function")

my_function()
``` 
Aynı fonksiyonu birden fazla kez çağırılabilir.

``` python
def my_function():
  print("Hello from a function")

my_function()
my_function()
my_function()
``` 

### Fonksiyon İsimleri

Fonksiyon isimleri Python’da değişken isimleriyle aynı kuralları takip eder:

- Fonksiyon ismi bir harf veya alt çizgi (_) ile başlamalıdır

- Fonksiyon ismi yalnızca harf, rakam ve alt çizgi içerebilir

- Fonksiyon isimleri büyük/küçük harfe duyarlıdır (ör. myFunction ve myfunction farklıdır)

``` python
calculate_sum()
_private_function()
myFunction2()
```  

Fonksiyonun ne yaptığını açıklayan anlamlı ve açıklayıcı isimler kullanmak iyi bir uygulamadır. 

#### Fonksiyon Kullanmanın Faydası

Örneğin, programınızda Fahrenheit’i Celsius’a birden fazla kez çevirmek istiyorsunuz.

Fonksiyon kullanmazsanız, aynı hesaplama kodunu defalarca tekrar yazmanız gerekir.

``` python
temp1 = 77
celsius1 = (temp1 - 32) * 5 / 9
print(celsius1)

temp2 = 95
celsius2 = (temp2 - 32) * 5 / 9
print(celsius2)

temp3 = 50
celsius3 = (temp3 - 32) * 5 / 9
print(celsius3)
```  

Fonksiyonlarla, kodu sadece bir kez yazarsınız ve istediğiniz kadar yeniden kullanılabilir.

``` python
def fahrenheit_to_celsius(fahrenheit):
  return (fahrenheit - 32) * 5 / 9

print(fahrenheit_to_celsius(77))
print(fahrenheit_to_celsius(95))
print(fahrenheit_to_celsius(50))
```  

### Return Değerleri

Fonksiyonlar, return ifadesi kullanarak çağrıldıkları koda veri gönderebilir.

Bir fonksiyon return ifadesine ulaştığında, çalışmayı durdurur ve sonucu geri döndürür.

``` python
def get_greeting():
  return "Hello from a function"

message = get_greeting()
print(message)
``` 
Geri döndürülen değeri doğrudan kullanılabilir.

``` python
def get_greeting():
  return "Hello from a function"

print(get_greeting())
```  

Bir fonksiyon return ifadesine sahip değilse, varsayılan olarak None döndürür. 

### pass İfadesi

Fonksiyon tanımları boş olamaz. Eğer kod içermeyen bir fonksiyon yer tutucu oluşturmanız gerekiyorsa, pass ifadesini kullanın.

``` python
def my_function():
  pass
``` 

pass ifadesi genellikle geliştirme sırasında kullanılır; önce yapıyı tanımlamanıza, sonra detayları daha sonra eklemenize olanak sağlar.

### Python Fonksiyon Argümanları

Fonksiyonlara bilgi argüman olarak iletilebilir.

Argümanlar, fonksiyon adından sonra, parantezler içinde belirtilir. İstediğiniz kadar argüman ekleyebilirsiniz; sadece virgülle ayırmanız yeterlidir.

Örneğin, aşağıdaki fonksiyon bir argüman (fname) alır. Fonksiyon çağrıldığında, bir isim iletilir ve fonksiyon bu ismi kullanarak ekrana yazdırır:

``` python
def my_function(fname):
  print(fname + " Refsnes")

my_function("Emil")
my_function("Tobias")
my_function("Linus")
``` 

### Parameters vs Arguments

Parametre ve argüman terimleri aynı şey için kullanılabilir: bir fonksiyona geçirilen bilgiler.

Bir fonksiyonun perspektifinden:

- Parameter (Parametre): Fonksiyon tanımında parantezlerin içinde listelenen değişkendir.

- Argument (Argüman): Fonksiyon çağrıldığında fonksiyona gönderilen gerçek değerdir.

``` python
def my_function(name): # name bir parametre
  print("Hello", name)

my_function("Emil") # "Emil" bir argüman
```  

#### Argüman Sayısı

Varsayılan olarak, bir fonksiyon doğru sayıda argüman ile çağrılmalıdır.

Eğer fonksiyonunuz 2 argüman bekliyorsa, onu tam olarak 2 argüman ile çağırmalısınız.

``` python
def my_function(fname, lname):
  print(fname + " " + lname)

my_function("Emil", "Refsnes")
``` 

Eğer fonksiyonu yanlış sayıda argüman ile çağırmaya çalışırsanız, bir hata alırsınız: 

``` python
def my_function(fname, lname):
  print(fname + " " + lname)

my_function("Emil")
# Bu fonksiyon 2 argüman bekliyor, ancak sadece 1 tane alıyor:
```

#### Varsayılan Parametre Değerleri

Parametrelere varsayılan değerler atayabilirsiniz. Eğer fonksiyon bir argüman olmadan çağrılırsa, varsayılan değeri kullanır:

``` python
def my_function(name = "friend"):
  print("Hello", name)

my_function("Emil")
my_function("Tobias")
my_function()
my_function("Linus")
``` 

country parametresi için varsayılan değer: 

``` python
def my_function(country = "Norway"):
  print("I am from", country)

my_function("Sweden")
my_function("India")
my_function()
my_function("Brazil")
```  

### Anahtar Kelime (Keyword) Argümanlar

Argümanları key = value sözdizimi ile gönderebilirsiniz.

``` python
def my_function(animal, name):
  print("I have a", animal)
  print("My", animal + "'s name is", name)

my_function(animal = "dog", name = "Buddy")
``` 

Bu şekilde, keyword argümanlar kullanıldığında, argümanların sırası önemli değildir 

``` python
def my_function(animal, name):
  print("I have a", animal)
  print("My", animal + "'s name is", name)

my_function(name = "Buddy", animal = "dog")
```  

“Keyword Arguments” ifadesi, Python dokümantasyonunda genellikle kısaca kwargs olarak geçer.

#### Positional Arguments

Bir fonksiyonu, anahtar kelime (keyword) kullanmadan argümanlarla çağırdığınızda, bu argümanlara positional arguments denir.

Positional arguments doğru sırada verilmelidir. 

``` python
def my_function(animal, name):
  print("I have a", animal)
  print("My", animal + "'s name is", name)

my_function("dog", "Buddy")")
``` 

Konumsal argümanlarda sıra önemlidir: 

``` python
def my_function(animal, name):
  print("I have a", animal)
  print("My", animal + "'s name is", name)

my_function("Buddy", "dog")
```  

#### Konumsal ve anahtar kelime argümanlarını karıştırma

Bir fonksiyon çağrısında konumsal ve anahtar kelime argümanlarını karıştırabilirsiniz.

Ancak, konumsal argümanlar anahtar kelime argümanlarından önce gelmelidir:

``` python
def my_function(animal, name, age):
  print("I have a", age, "year old", animal, "named", name)

my_function("dog", name = "Buddy", age = 5)
``` 

#### Farklı Veri Tiplerini Gönderme

Bir fonksiyona herhangi bir veri tipi (string, sayı, liste, sözlük vb.) argüman olarak gönderilebilir.

Veri tipi, fonksiyon içinde korunur:

``` python
def my_function(fruits):
  for fruit in fruits:
    print(fruit)

my_fruits = ["apple", "banana", "cherry"]
my_function(my_fruits)
```  

``` python
def my_function(person):
  print("Name:", person["name"])
  print("Age:", person["age"])

my_person = {"name": "Emil", "age": 25}
my_function(my_person)
```  

### Return Değerleri

Fonksiyonlar, return ifadesi kullanarak değer döndürebilir:

``` python
def my_function(x, y):
  return x + y

result = my_function(5, 3)
print(result)
``` 

#### Farklı Veri Tiplerini Döndürme

Fonksiyonlar, listeler, demetler (tuples), sözlükler (dictionaries) ve diğer veri tipleri dahil olmak üzere herhangi bir veri tipini döndürebilir.

``` python
def my_function():
  return ["apple", "banana", "cherry"]

fruits = my_function()
print(fruits[0])
print(fruits[1])
print(fruits[2])
```  

Bir fonksiyonun tuple döndürmesi: 

``` python
def my_function():
  return (10, 20)

x, y = my_function()
print("x:", x)
print("y:", y)
``` 

### Sadece Konum (Positional-Only) Argümanlar

Bir fonksiyonun SADECE konum (positional) argüman almasını belirtebilirsiniz.

Bunu yapmak için argümanların sonuna , / ekleyin:

``` python
def my_function(name, /):
  print("Hello", name)

my_function("Emil")
```  

, / olmadan, fonksiyon aslında positional argüman beklese bile keyword argümanlarını kullanmanıza izin verir. 

``` python
def my_function(name):
  print("Hello", name)

my_function(name = "Emil")
``` 

, / ile, keyword argümanlarını kullanmaya çalışırsanız bir hata alırsınız.

``` python
def my_function(name, /):
  print("Hello", name)

my_function(name = "Emil")
``` 

### Sadece Anahtar Kelime (Keyword-Only) Argümanlar

Bir fonksiyonun yalnızca keyword argüman almasını belirtmek için argümanların önüne ***,** ekleyin:

``` python
def my_function(*, name):
  print("Hello", name)

my_function(name = "Emil")
``` 
**"*,"** olmadan, fonksiyon keyword argüman beklese bile positional argüman kullanmanıza izin verilir:

``` python
def my_function(name):
  print("Hello", name)

my_function("Emil")
```  
***,** ile, positional argüman kullanmaya çalışırsanız hata alırsınız:

``` python
def my_function(*, name):
  print("Hello", name)

my_function("Emil")
```  
### Konum (Positional-Only) ve Anahtar Kelime (Keyword-Only) Argümanları Birleştirme

Aynı fonksiyonda her iki argüman türünü de kullanabilirsiniz.

/ öncesindeki argümanlar sadece konum (positional-only), * sonrasındaki argümanlar ise sadece anahtar kelime (keyword-only) argümanlardır:

``` python
def my_function(a, b, /, *, c, d):
  return a + b + c + d

result = my_function(5, 10, c = 15, d = 20)
print(result)
```  

### Python *args ve **kwargs

Varsayılan olarak, bir fonksiyon doğru sayıda argüman ile çağrılmalıdır.

Ancak bazen fonksiyonunuza kaç argüman gönderileceğini bilmeyebilirsiniz.

*args ve **kwargs, fonksiyonların bilinmeyen sayıda argümanı kabul etmesini sağlar.

### Rastgele Argümanlar - *args

Fonksiyonunuza kaç argüman gönderileceğini bilmiyorsanız, parametre adının önüne * ekleyin.

Bu şekilde fonksiyon, argümanları bir tuple olarak alır ve öğelere buna göre erişebilir.

``` python
def my_function(*kids):
  print("The youngest child is " + kids[2])

my_function("Emil", "Tobias", "Linus")
``` 
Rastgele Argümanlar, Python dokümantasyonunda genellikle *args olarak kısaltılır. 

### *args Nedir?

*args parametresi, bir fonksiyonun herhangi sayıda konum (positional) argüman almasına izin verir.

Fonksiyon içinde, args tüm gönderilen argümanları içeren bir tuple haline gelir:

``` python
def my_function(*args):
  print("Type:", type(args))
  print("First argument:", args[0])
  print("Second argument:", args[1])
  print("All arguments:", args)

my_function("Emil", "Tobias", "Linus")
```  

#### *args ile Normal Argümanları Kullanmak

Normal parametreleri *args ile birleştirebilirsiniz.

Normal parametreler, *args’tan önce gelmelidir:

``` python
def my_function(greeting, *names):
  for name in names:
    print(greeting, name)

my_function("Hello", "Emil", "Tobias", "Linus")
```  

Bu örnekte, "Hello" greeting parametresine atanır ve geri kalanlar names içinde toplanır. 

#### *args ile pratik örnekler

*args, esnek fonksiyonlar oluşturmak istediğinizde faydalıdır.

Herhangi bir sayıda değerin toplamını hesaplayan bir fonksiyon:

``` python
def my_function(*numbers):
  total = 0
  for num in numbers:
    total += num
  return total

print(my_function(1, 2, 3))
print(my_function(10, 20, 30, 40))
print(my_function(5))
``` 

En yüksek değeri bulma: 

``` python
def my_function(*numbers):
  if len(numbers) == 0:
    return None
  max_num = numbers[0]
  for num in numbers:
    if num > max_num:
      max_num = num
  return max_num

print(my_function(3, 7, 2, 9, 1))
``` 

### Belirsiz Sayıda Anahtar Argümanlar – **kwargs

Eğer fonksiyonuna kaç tane keyword argument gönderileceğini bilmiyorsan, parametre adının önüne iki yıldız (**) koy.

Bu şekilde fonksiyon, argümanların bir dictionary’sini alır ve öğelere (items) buna göre erişebilir.

``` python
def my_function(**kid):
  print("His last name is " + kid["lname"])

my_function(fname = "Tobias", lname = "Refsnes")
```  

Belirsiz Sayıda Anahtar Argümanlar, Python dokümantasyonunda genellikle **kwargs şeklinde kısaltılır. 

### **kwargs nedir?

**kwargs parametresi, bir fonksiyonun herhangi sayıda keyword argument kabul etmesine izin verir.

Fonksiyonun içinde, kwargs tüm keyword argument’leri içeren bir dictionary haline gelir.

``` python
def my_function(**myvar):
  print("Type:", type(myvar))
  print("Name:", myvar["name"])
  print("Age:", myvar["age"])
  print("All data:", myvar)

my_function(name = "Tobias", age = 30, city = "Bergen")
```  

### **kwargs ile Normal Parametreleri Kullanmak

Normal parametreleri **kwargs ile birleştirebilirsin.

Normal parametreler, **kwargs’dan önce gelmelidir.

``` python
def my_function(username, **details):
  print("Username:", username)
  print("Additional details:")
  for key, value in details.items():
    print(" ", key + ":", value)

my_function("emil123", age = 25, city = "Oslo", hobby = "coding")
``` 

### *args ve **kwargs’ı Birleştirmek
Aynı fonksiyonda hem *args hem de **kwargs kullanabilirsin.

Sıralama şu şekilde olmalıdır:

1. Normal parametreler

2. *args

3. **kwargs

``` python
def my_function(title, *args, **kwargs):
  print("Title:", title)
  print("Positional arguments:", args)
  print("Keyword arguments:", kwargs)

my_function("User Info", "Emil", "Tobias", age = 25, city = "Oslo")
```  

### Argümanları Açmak (Unpacking)

"*" ve ** operatörleri, fonksiyon çağrılırken de kullanılabilir; bir listeyi veya dictionary’yi ayrı argümanlara genişletmek (unpack) için.

### Listeyi * ile Açmak

Eğer değerler bir listede saklanmışsa, * kullanarak bunları bireysel argümanlar olarak açabilirsin:

``` python
def my_function(a, b, c):
  return a + b + c

numbers = [1, 2, 3]
result = my_function(*numbers)
print(result)
```  
### Dictionary’leri ** ile Açmak

Eğer keyword argument’ler bir dictionary içinde saklanmışsa, ** kullanarak bunları ayrı argümanlar olarak açabilirsin:

``` python
def my_function(fname, lname):
  print("Hello", fname, lname)

person = {"fname": "Emil", "lname": "Refsnes"}
my_function(**person)
```

## Python Kullanıcı Girdisi 

Python, kullanıcıdan girdi alınmasına izin verir.

Bu, kullanıcıdan bir girdi istememize olanak sağlar.

Aşağıdaki örnek, kullanıcının adını sorar ve kullanıcı bir ad girdiğinde, bu ad ekrana yazdırılır: 

``` python
print("Enter your name:")
name = input()
print(f"Hello {name}")
``` 

Python, input() fonksiyonuna geldiğinde çalışmayı durdurur ve kullanıcı bir girdi verdiğinde devam eder. 

### Prompt Kullanmak

Yukarıdaki örnekte, kullanıcı adını yeni bir satıra girmek zorundaydı.

Python’un input() fonksiyonunun bir prompt parametresi vardır; bu parametre, kullanıcı girdisinin aynı satırında gösterebileceğin bir mesaj işlevi görür: 

``` python
name = input("Enter your name:")
print(f"Hello {name}")
``` 

### Birden Fazla Girdi

İstediğin kadar girdi ekleyebilirsin; Python, her birinde çalışmayı durdurur ve kullanıcı girişi bekler: 

``` python
name = input("Enter your name:")
print(f"Hello {name}")
fav1 = input("What is your favorite animal:")
fav2 = input("What is your favorite color:")
fav3 = input("What is your favorite number:")
print(f"Do you want a {fav2} {fav1} with {fav3} legs?"))
``` 

### Sayı Girdisi

Kullanıcıdan alınan girdi, her zaman string olarak kabul edilir. Yukarıdaki örnekte sayı girsen bile, Python bunu string olarak işler.

Girdiyi bir sayıya dönüştürmek için float() fonksiyonunu kullanabilirsin.

``` python
x = input("Enter a number:")

y = math.sqrt(float(x))

print(f"The square root of {x} is {y}")
```

### Girdiyi Doğrulamak (Validate Input)

Kullanıcıdan alınan girdiyi doğrulamak iyi bir uygulamadır. Yukarıdaki örnekte, kullanıcı sayı dışında bir şey girerse hata oluşur.

Hata oluşmasını önlemek için girdiyi test edebilir, eğer sayı değilse kullanıcıya "Yanlış giriş, lütfen tekrar deneyin" gibi bir mesaj gösterebilir ve yeni bir girdi yapmasına izin verebilirsin.

``` python
y = True
while y == True:
  x = input("Enter a number:")
  try:
    x = float(x);
    y = False
  except:
    print("Wrong input, please try again.")

print("Thank you!")
``` 

## Python Sınıfları (Classes) ve Nesneleri (Objects) 

 Python, nesne yönelimli bir programlama dilidir.

Python’da neredeyse her şey bir nesnedir (object) ve kendi özellikleri (properties) ve metotları (methods) vardır.

Sınıf (Class), bir nesne oluşturucu ya da nesne yaratmak için bir “taslak (blueprint)” gibidir.

### Bir Sınıf Oluşturmak

Sınıf oluşturmak için class anahtar kelimesini kullan:

``` python
class MyClass:
  x = 5
``` 

### Nesne Oluşturmak

Artık MyClass adlı sınıfı kullanarak nesneler oluşturabiliriz:

``` python
p1 = MyClass()
print(p1.x)
``` 

### Nesneleri Silmek
Nesneleri del anahtar kelimesini kullanarak silebilirsin:

``` python
del p1
``` 

### Birden Fazla Nesne

Aynı sınıftan birden fazla nesne oluşturabilirsin:

``` python
p1 = MyClass()
p2 = MyClass()
p3 = MyClass()

print(p1.x)
print(p2.x)
print(p3.x)
``` 

Not: Her nesne bağımsızdır ve sınıfın özelliklerinin kendi kopyasına sahiptir 

### pass İfadesi

Sınıf tanımları boş olamaz, ancak bir nedenle sınıf tanımının içeriği yoksa, hata almamak için pass ifadesini kullanabilirsin. 

``` python
class Person:
  pass
``` 
### Python __init__() Metodu

Tüm sınıfların, sınıf başlatıldığında (initiated) her zaman çalıştırılan yerleşik bir metodu vardır: `__init__()`().

`__init__()`() metodu, nesne özelliklerine değer atamak veya nesne oluşturulurken gerekli işlemleri yapmak için kullanılır. 

``` python
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

p1 = Person("Emil", 36)

print(p1.name)
print(p1.age)
``` 

Not: `__init__()` metodu, sınıf her yeni nesne oluşturulduğunda otomatik olarak çağrılır.

### Neden `__init__()` Kullanılır?
`__init__()` metodu olmadan, her nesne için özellikleri manuel olarak ayarlaman gerekirdi:

``` python
class Person:
  pass

p1 = Person()
p1.name = "Tobias"
p1.age = 25

print(p1.name)
print(p1.age)
``` 
`__init__()` kullanmak, nesneleri başlangıç değerleriyle oluşturmayı kolaylaştırır:

``` python
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

p1 = Person("Linus", 28)

print(p1.name)
print(p1.age)
``` 

### `__init__()` İçinde Varsayılan Değerler
`__init__()` metodundaki parametreler için varsayılan değerler de atayabilirsin:

``` python
class Person:
  def __init__(self, name, age=18):
    self.name = name
    self.age = age

p1 = Person("Emil")
p2 = Person("Tobias", 25)

print(p1.name, p1.age)
print(p2.name, p2.age)
``` 

### Birden Fazla Parametre

`__init__()` metodu, ihtiyaç duyduğun kadar parametre alabilir:

``` python
class Person:
  def __init__(self, name, age, city, country):
    self.name = name
    self.age = age
    self.city = city
    self.country = country

p1 = Person("Linus", 30, "Oslo", "Norway")

print(p1.name)
print(p1.age)
print(p1.city)
print(p1.country)
``` 

### Python self Parametresi

self parametresi, sınıfın mevcut örneğine (instance) bir referanstır.

Sınıfa ait özelliklere (properties) ve metotlara (methods) erişmek için kullanılır.

``` python
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

  def greet(self):
    print("Hello, my name is " + self.name)

p1 = Person("Emil", 25)
p1.greet()
``` 
Not: self parametresi, sınıftaki herhangi bir metodun ilk parametresi olmalıdır. 

### Neden self Kullanılır?

self kullanılmazsa, Python hangi nesnenin özelliklerine erişmek istediğini bilemez:

``` python
class Person:
  def __init__(self, name):
    self.name = name

  def printname(self):
    print(self.name)

p1 = Person("Tobias")
p2 = Person("Linus")

p1.printname()
p2.printname()
``` 

### self Mutlaka “self” Olarak Adlandırılmak Zorunda Değil

Parametreyi self olarak adlandırmak zorunda değilsin, istediğin herhangi bir isim verebilirsin; ancak sınıftaki herhangi bir metodun ilk parametresi olması gerekir:

``` python
class Person:
  def __init__(myobject, name, age):
    myobject.name = name
    myobject.age = age

  def greet(abc):
    print("Hello, my name is " + abc.name)

p1 = Person("Emil", 36)
p1.greet()
``` 

Not: Farklı bir isim kullanabilirsin, ancak Python’da konvansiyon olarak self kullanılması şiddetle tavsiye edilir; bu, kodunun başkaları tarafından daha okunabilir olmasını sağlar. 

### self ile Özelliklere Erişmek

Sınıfın herhangi bir özelliğine self kullanarak erişebilirsin:

``` python
class Car:
  def __init__(self, brand, model, year):
    self.brand = brand
    self.model = model
    self.year = year

  def display_info(self):
    print(f"{self.year} {self.brand} {self.model}")

car1 = Car("Toyota", "Corolla", 2020)
car1.display_info()
``` 

### self ile Metotları Çağırmak

Sınıfın içindeki diğer metotları self kullanarak çağırabilirsin:

``` python
class Person:
  def __init__(self, name):
    self.name = name

  def greet(self):
    return "Hello, " + self.name

  def welcome(self):
    message = self.greet()
    print(message + "! Welcome to our website.")

p1 = Person("Tobias")
p1.welcome()
``` 

### Python Sınıf Özellikleri (Class Properties)

Özellikler (Properties), bir sınıfa ait değişkenlerdir. Sınıftan oluşturulan her nesne için veri saklarlar.

``` python
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

p1 = Person("Emil", 36)

print(p1.name)
print(p1.age)
```  

### Özelliklere Erişmek

Nesne özelliklerine nokta notasyonu (dot notation) kullanarak erişebilirsin:

``` python
class Car:
  def __init__(self, brand, model):
    self.brand = brand
    self.model = model

car1 = Car("Toyota", "Corolla")

print(car1.brand)
print(car1.model)
```  

### Özellikleri Değiştirmek

Nesnelerin özelliklerinin değerlerini değiştirebilirsin:

``` python
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

p1 = Person("Tobias", 25)
print(p1.age)

p1.age = 26
print(p1.age)
``` 

ChatGPT:

### Özellikleri Silmek

Nesnelerin özelliklerini del anahtar kelimesi ile silebilirsin:

``` python
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

p1 = Person("Linus", 30)

del p1.age

print(p1.name)
``` 

### Sınıf Özellikleri vs Nesne Özellikleri

`__init__()` içinde tanımlanan özellikler, her nesneye ait olur (instance properties).

Metotların dışında tanımlanan özellikler ise sınıfın kendisine ait olur (class properties) ve tüm nesneler tarafından paylaşılır.

``` python
class Person:
  species = "Human"

  def __init__(self, name):
    self.name = name 

p1 = Person("Emil")
p2 = Person("Tobias")

print(p1.name)
print(p2.name)
print(p1.species)
print(p2.species)
```

### Sınıf Özelliklerini Değiştirmek

Bir sınıf özelliğini değiştirdiğinde, bu tüm nesneleri etkiler:

``` python
class Person:
  lastname = ""

  def __init__(self, name):
    self.name = name

p1 = Person("Linus")
p2 = Person("Emil")

Person.lastname = "Refsnes"

print(p1.lastname)
print(p2.lastname)
``` 

### Yeni Özellikler Eklemek

Mevcut nesnelere yeni özellikler ekleyebilirsin:

``` python
class Person:
  def __init__(self, name):
    self.name = name

p1 = Person("Tobias")

p1.age = 25
p1.city = "Oslo"

print(p1.name)
print(p1.age)
print(p1.city)
``` 

Not: Bu şekilde özellik eklemek, yalnızca o özel nesneye ekler; sınıftaki tüm nesnelere eklenmez. 

### Python Sınıf Metotları (Class Methods)

Metotlar (Methods), bir sınıfa ait fonksiyonlardır. Sınıftan oluşturulan nesnelerin davranışlarını tanımlarlar.

``` python
class Person:
  def __init__(self, name):
    self.name = name

  def greet(self):
    print("Hello, my name is " + self.name)

p1 = Person("Emil")
p1.greet()
``` 

Not: Tüm metotların ilk parametresi self olmalıdır.

### Parametreli Metotlar

Metotlar, normal fonksiyonlar gibi parametre alabilir:

``` python
class Calculator:
  def add(self, a, b):
    return a + b

  def multiply(self, a, b):
    return a * b

calc = Calculator()
print(calc.add(5, 3))
print(calc.multiply(4, 7))
```  
### Özelliklere Erişen Metotlar

Metotlar, self kullanarak nesne özelliklerine erişebilir ve onları değiştirebilir:

``` python
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

  def get_info(self):
    return f"{self.name} is {self.age} years old"

p1 = Person("Tobias", 28)
print(p1.get_info())
```  

### Özellikleri Değiştiren Metotlar

Metotlar, bir nesnenin özelliklerini değiştirebilir:

``` python
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

  def celebrate_birthday(self):
    self.age += 1
    print(f"Happy birthday! You are now {self.age}")

p1 = Person("Linus", 25)
p1.celebrate_birthday()
p1.celebrate_birthday()
``` 

### ``__str__``() Metodu

``__str__()`` metodu, nesne ekrana yazdırıldığında neyin döndürüleceğini kontrol eden özel bir metottur.`

``` python
#__str__() metodu olmadan:
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

p1 = Person("Emil", 36)
print(p1)
``` 

``` python
#__str__() metodu ile:
class Person:
  def __init__(self, name, age):
    self.name = name
    self.age = age

  def __str__(self):
    return f"{self.name} ({self.age})"

p1 = Person("Tobias", 36)
print(p1)
```

### Birden Fazla Metot

Bir sınıf, birlikte çalışan birden fazla metoda sahip olabilir:

``` python
class Playlist:
  def __init__(self, name):
    self.name = name
    self.songs = []

  def add_song(self, song):
    self.songs.append(song)
    print(f"Added: {song}")

  def remove_song(self, song):
    if song in self.songs:
      self.songs.remove(song)
      print(f"Removed: {song}")

  def show_songs(self):
    print(f"Playlist '{self.name}':")
    for song in self.songs:
      print(f"- {song}")

my_playlist = Playlist("Favorites")
my_playlist.add_song("Bohemian Rhapsody")
my_playlist.add_song("Stairway to Heaven")
my_playlist.show_songs()
``` 

### Metotları Silmek

Sınıftaki metotları del anahtar kelimesi ile silebilirsin:

``` python
class Person:
  def __init__(self, name):
    self.name = name

  def greet(self):
    print("Hello!")

p1 = Person("Emil")

del Person.greet

p1.greet()
```